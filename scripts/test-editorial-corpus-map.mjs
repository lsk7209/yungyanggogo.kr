import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { spawnSync } from "node:child_process";
import path from "node:path";

const root = process.cwd();
const generation = spawnSync(process.execPath, [path.join(root, "scripts", "map-blog-editorial-corpus.mjs")], {
  cwd: root,
  encoding: "utf8",
});
assert.equal(generation.status, 0, generation.stderr || generation.stdout);
const script = readFileSync(path.join(root, "scripts", "map-blog-editorial-corpus.mjs"), "utf8");
const report = JSON.parse(readFileSync(path.join(root, "docs", "quality", "editorial-corpus-map-2026-09-12.json"), "utf8"));
const assertions = [];

function check(condition, message) {
  assert.ok(condition, message);
  assertions.push(message);
}

check(report.summary.files === 7, "map covers all seven generated-content files");
check(report.summary.posts === 700 && report.queue.length === 700, "map covers all 700 legacy posts exactly once");
check(new Set(report.queue.map((row) => row.slug)).size === 700, "map contains 700 unique legacy slugs");
check(report.summary.clusters === 140 && report.clusters.length === 140, "map identifies 140 topic clusters");
check(report.summary.completeFiveVariantClusters === 140, "every cluster has the five expected variants");
check(report.clusters.every((cluster) => cluster.rowCount === 5), "every cluster contains exactly five rows");
check(report.summary.clustersWithIdenticalExpandedKeywords === 140, "every cluster records the shared expanded-keyword risk");
check(report.summary.clustersWithIdenticalSourceSets === 140, "every cluster records the shared source-set risk");
check(report.summary.recognizedCandidatePackets === 7, "only seven complete pending-review candidate packets are recognized");
check(report.summary.uniqueExactTitles === 700 && report.summary.uniqueNormalizedTitles === 700, "title uniqueness alone cannot explain semantic overlap");
check(report.summary.exactTitleDuplicateRows === 0 && report.summary.normalizedTitleDuplicateRows === 0, "legacy titles have no exact or normalized duplicates");
check(report.summary.uniqueExactHeadingSignatures === 580 && report.summary.uniqueNormalizedHeadingSignatures === 580, "heading sequence inventory records 580 distinct signatures");
check(report.summary.normalizedHeadingDuplicateGroups === 60 && report.summary.normalizedHeadingDuplicateRows === 180, "sixty repeated heading groups affect 180 rows");
check(report.summary.uniqueContractShapeSignatures === 34, "legacy corpus collapses to 34 contract-shape signatures");
check(report.summary.rowsWithKnownLanguageDefects > 0, "queue exposes known Korean language defects per row");
check(Object.values(report.summary.suffixCounts).every((count) => count === 140), "each of five suffixes appears 140 times");
check(report.queue.every((row) => ["basis", "mistake", "compare", "routine", "faq"].includes(row.variant)), "every queue row exposes its parsed variant");
check(Object.values(report.summary.suffixStructureMap).every((types) => types.length === 1), "each suffix maps to one generated structure type");
check(report.summary.decisionCounts.replace_after_human_approval === 7, "seven basis rows are ready to await human approval");
check((report.summary.decisionCounts.revise_candidate_before_human_review ?? 0) === 0, "no basis row remains in candidate revision");
check(report.summary.decisionCounts.reject_cannibalizing_sibling === 293, "twenty-eight candidate siblings and two hundred sixty-five cross-cluster duplicates are rejected");
check((report.summary.decisionCounts.contract_review_required ?? 0) === 0, "no unreviewed basis row remains in the legacy corpus");
check((report.summary.decisionCounts.hold_for_cluster_contract_review ?? 0) === 0, "no sibling remains behind an unresolved contract review");
check(report.summary.decisionCounts.defer_research_needed === 80, "eighty distinct bases are deferred until their evidence gates are satisfied");
check(report.summary.decisionCounts.hold_for_research_needed === 320, "four siblings per research-deferred owner remain held behind its evidence gate");
check(report.clusters.find((cluster) => cluster.clusterKey === "bagel-carbs")?.clusterDecision === "merged_into_existing_topic_owner", "bagel-carbs cluster is merged into an existing topic owner");
check(report.queue.filter((row) => row.clusterKey === "bagel-carbs").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "phase6t-bagel-basis"), "all bagel-carbs rows merge into phase6t-bagel-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-noodle-topping")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t noodle-topping cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-noodle-topping").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "noodle-topping-calorie-basis"), "all phase6t noodle-topping rows merge into noodle-topping-calorie-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cheese")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t cheese cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cheese").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "cheese-protein-fat-basis"), "all phase6t cheese rows merge into cheese-protein-fat-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-oatmeal")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t oatmeal cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-oatmeal").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "oatmeal-fiber-basis"), "all phase6t oatmeal rows merge into oatmeal-fiber-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-jam")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t jam cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-jam").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "jam-sugar-basis"), "all phase6t jam rows merge into jam-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-rice-cake-snack")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t rice-cake snack cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-rice-cake-snack").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "rice-cake-snack-carbs-basis"), "all phase6t rice-cake snack rows merge into rice-cake-snack-carbs-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-coffee-drink")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t coffee-drink cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-coffee-drink").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "coffee-drink-sugar-basis"), "all phase6t coffee-drink rows merge into coffee-drink-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cup-soup")?.clusterDecision === "deferred_pending_research", "phase6t cup-soup remains behind its narrower preparation-recalculation research gate");
check(report.queue.find((row) => row.slug === "phase6t-cup-soup-basis")?.editorialDecision === "defer_research_needed", "phase6t cup-soup basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cup-soup" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-cup-soup-basis"), "phase6t cup-soup siblings remain held behind their preparation-recalculation owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cold-noodle-sauce")?.clusterDecision === "deferred_pending_research", "phase6t cold-noodle sauce is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "phase6t-cold-noodle-sauce-basis")?.editorialDecision === "defer_research_needed", "cold-noodle sauce basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cold-noodle-sauce" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-cold-noodle-sauce-basis"), "cold-noodle sauce siblings remain held behind the research-needed basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cereal-bar")?.clusterDecision === "deferred_pending_research", "phase6t cereal bar is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "phase6t-cereal-bar-basis")?.editorialDecision === "defer_research_needed", "cereal-bar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cereal-bar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-cereal-bar-basis"), "cereal-bar siblings remain held behind the research-needed basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-protein-bar")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t protein-bar cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-protein-bar").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "protein-bar-fat-basis"), "all phase6t protein-bar rows merge into protein-bar-fat-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-instant-curry")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t instant-curry cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-instant-curry").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "instant-curry-calorie-basis"), "all phase6t instant-curry rows merge into instant-curry-calorie-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-sparkling-water-drink")?.clusterDecision === "deferred_pending_research", "phase6t sparkling-water drink is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "phase6t-sparkling-water-drink-basis")?.editorialDecision === "defer_research_needed", "sparkling-water basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-sparkling-water-drink" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-sparkling-water-drink-basis"), "sparkling-water siblings remain held behind the research-needed basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-table-sauce")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t table-sauce cluster is merged into the earlier generic sauce owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-table-sauce").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "sauce-sodium-basis"), "all phase6t table-sauce rows merge into sauce-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-sweetener-snack")?.clusterDecision === "deferred_pending_research", "phase6t sweetener snack is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "phase6t-sweetener-snack-basis")?.editorialDecision === "defer_research_needed", "sweetener-snack basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-sweetener-snack" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-sweetener-snack-basis"), "sweetener-snack siblings remain held behind the research-needed basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-unsweetened-yogurt")?.clusterDecision === "deferred_pending_research", "phase6t unsweetened yogurt is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "phase6t-unsweetened-yogurt-basis")?.editorialDecision === "defer_research_needed", "unsweetened-yogurt basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-unsweetened-yogurt" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-unsweetened-yogurt-basis"), "unsweetened-yogurt siblings remain held behind the research-needed basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-greek-yogurt")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t Greek-yogurt cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-greek-yogurt").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "greek-yogurt-protein-basis"), "all phase6t Greek-yogurt rows merge into greek-yogurt-protein-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-soy-milk")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t soy-milk cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-soy-milk").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "soy-milk-sugar-basis"), "all phase6t soy-milk rows merge into soy-milk-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-triangle-kimbap")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t triangle-kimbap cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-triangle-kimbap").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "triangle-kimbap-calorie-basis"), "all phase6t triangle-kimbap rows merge into triangle-kimbap-calorie-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cereal")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t cereal cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cereal").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "cereal-sugar-basis"), "all phase6t cereal rows merge into cereal-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-icecream")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t ice-cream cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-icecream").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "icecream-satfat-basis"), "all phase6t ice-cream rows merge into icecream-satfat-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-bakery-snack")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t bakery-snack cluster is merged into the researched bakery owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-bakery-snack").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "bakery-snack-serving-basis"), "all phase6t bakery-snack rows merge into bakery-snack-serving-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-sports-drink")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t sports-drink cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-sports-drink").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "sports-drink-sugar-basis"), "all phase6t sports-drink rows merge into sports-drink-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-tteokbokki-kit")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t tteokbokki meal-kit cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-tteokbokki-kit").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "tteokbokki-mealkit-sodium-basis"), "all phase6t tteokbokki meal-kit rows merge into tteokbokki-mealkit-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-dried-sweet-potato")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t dried sweet-potato cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-dried-sweet-potato").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "dried-sweet-potato-sugar-basis"), "all phase6t dried sweet-potato rows merge into dried-sweet-potato-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-granola")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t granola cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-granola").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "granola-fat-basis"), "all phase6t granola rows merge into granola-fat-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-flavored-milk")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t flavored-milk cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-flavored-milk").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "flavored-milk-sugar-basis"), "all phase6t flavored-milk rows merge into flavored-milk-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-meal-drink")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t meal-drink cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-meal-drink").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "meal-replacement-drink-basis"), "all phase6t meal-drink rows merge into meal-replacement-drink-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-powder-drink")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t powder-drink cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-powder-drink").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "powder-drink-sugar-basis"), "all phase6t powder-drink rows merge into powder-drink-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-fruit-veg-juice")?.clusterDecision === "deferred_pending_research", "phase6t fruit/vegetable juice is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "phase6t-fruit-veg-juice-basis")?.editorialDecision === "defer_research_needed", "fruit/vegetable juice basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "phase6t-fruit-veg-juice" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "phase6t-fruit-veg-juice-basis"), "fruit/vegetable juice siblings remain held behind the research-needed basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-protein-shake")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t protein-shake cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-protein-shake").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "protein-shake-sugar-basis"), "all phase6t protein-shake rows merge into protein-shake-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-canned-corn")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t canned-corn cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-canned-corn").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "canned-corn-sugar-basis"), "all phase6t canned-corn rows merge into canned-corn-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-salad-dressing")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t salad-dressing cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-salad-dressing").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "salad-dressing-sugar-basis"), "all phase6t salad-dressing rows merge into salad-dressing-sugar-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-mixed-nuts")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t mixed-nuts cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-mixed-nuts").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "nuts-serving-size-basis"), "all phase6t mixed-nuts rows merge into nuts-serving-size-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-beef-jerky")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t beef-jerky cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-beef-jerky").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "beef-jerky-sodium-basis"), "all phase6t beef-jerky rows merge into beef-jerky-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-porridge")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t porridge cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-porridge").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "porridge-soup-sodium-basis"), "all phase6t porridge rows merge into porridge-soup-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cracker")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t cracker cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cracker").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "cracker-sodium-basis"), "all phase6t cracker rows merge into cracker-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-sandwich")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t sandwich cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-sandwich").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "sandwich-sodium-basis"), "all phase6t sandwich rows merge into sandwich-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-seaweed-snack")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t seaweed-snack cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-seaweed-snack").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "seaweed-snack-sodium-basis"), "all phase6t seaweed-snack rows merge into seaweed-snack-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cup-noodle")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t cup-noodle cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cup-noodle").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "cup-noodle-sodium-basis"), "all phase6t cup-noodle rows merge into cup-noodle-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-tofu-snack")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t tofu-snack cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-tofu-snack").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "tofu-snack-protein-basis"), "all phase6t tofu-snack rows merge into tofu-snack-protein-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-instant-udon")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t instant-udon cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-instant-udon").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "instant-udon-sodium-basis"), "all phase6t instant-udon rows merge into instant-udon-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-canned-tuna")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t canned-tuna cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-canned-tuna").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "canned-tuna-sodium-basis"), "all phase6t canned-tuna rows merge into canned-tuna-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-ham-sausage")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t ham-sausage cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-ham-sausage").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "ham-sausage-sodium-basis"), "all phase6t ham-sausage rows merge into ham-sausage-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-frozen-hotdog")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t frozen-hotdog cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-frozen-hotdog").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "frozen-hotdog-satfat-basis"), "all phase6t frozen-hotdog rows merge into frozen-hotdog-satfat-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-frozen-dumpling")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t frozen-dumpling cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-frozen-dumpling").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "dumpling-sodium-calorie-basis"), "all phase6t frozen-dumpling rows merge into dumpling-sodium-calorie-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-chicken-sausage")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t chicken-sausage cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-chicken-sausage").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "chicken-sausage-sodium-basis"), "all phase6t chicken-sausage rows merge into chicken-sausage-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-kimchi")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t kimchi cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-kimchi").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "kimchi-sodium-label-basis"), "all phase6t kimchi rows merge into kimchi-sodium-label-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-cold-lunchbox")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t cold-lunchbox cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-cold-lunchbox").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "lunchbox-calorie-basis"), "all phase6t cold-lunchbox rows merge into lunchbox-calorie-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-fried-rice")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t fried-rice cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-fried-rice").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "fried-rice-meal-sodium-basis"), "all phase6t fried-rice rows merge into fried-rice-meal-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-fishcake-soup-kit")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t fishcake-soup-kit cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-fishcake-soup-kit").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "fishcake-soup-mealkit-basis"), "all phase6t fishcake-soup-kit rows merge into fishcake-soup-mealkit-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-kids-drink")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t kids-drink cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-kids-drink").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "kids-drink-100ml-basis"), "all phase6t kids-drink rows merge into kids-drink-100ml-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-pasta-sauce")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t pasta-sauce cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-pasta-sauce").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "pasta-sauce-sodium-basis"), "all phase6t pasta-sauce rows merge into pasta-sauce-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-low-sodium-soup-base")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t low-sodium soup-base cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-low-sodium-soup-base").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "soup-base-sodium-basis"), "all phase6t low-sodium soup-base rows merge into soup-base-sodium-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-instant-rice")?.clusterDecision === "merged_into_existing_topic_owner", "phase6t instant-rice cluster is merged into the earlier topic owner");
check(report.queue.filter((row) => row.clusterKey === "phase6t-instant-rice").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "instant-rice-carbs-basis"), "all phase6t instant-rice rows merge into instant-rice-carbs-basis");
check(report.clusters.find((cluster) => cluster.clusterKey === "sandwich-sodium")?.clusterDecision === "deferred_pending_research", "sandwich-sodium owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "sandwich-sodium-basis")?.editorialDecision === "defer_research_needed", "sandwich-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "sandwich-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "sandwich-sodium-basis"), "sandwich-sodium siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "salad-topping-calorie")?.clusterDecision === "merged_into_existing_topic_owner", "legacy salad-topping calorie cluster is merged into the researched candidate owner");
check(report.queue.filter((row) => row.clusterKey === "salad-topping-calorie").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "phase6t-salad-topping-basis"), "all legacy salad-topping calorie rows merge into the researched phase6t candidate");
check(report.clusters.find((cluster) => cluster.clusterKey === "protein-bar-fat")?.clusterDecision === "deferred_pending_research", "protein-bar-fat owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "protein-bar-fat-basis")?.editorialDecision === "defer_research_needed", "protein-bar-fat basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "protein-bar-fat" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "protein-bar-fat-basis"), "protein-bar-fat siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "pudding-sugar")?.clusterDecision === "deferred_pending_research", "pudding-sugar cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "pudding-sugar-basis")?.editorialDecision === "defer_research_needed", "pudding-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "pudding-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "pudding-sugar-basis"), "pudding-sugar siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "dried-fruit-sugar")?.clusterDecision === "merged_into_existing_topic_owner", "legacy dried-fruit sugar cluster is merged into the researched candidate owner");
check(report.queue.filter((row) => row.clusterKey === "dried-fruit-sugar").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "phase6t-dried-fruit-basis"), "all legacy dried-fruit sugar rows merge into the researched phase6t candidate");
check(report.clusters.find((cluster) => cluster.clusterKey === "phase6t-dried-sweet-potato")?.clusterDecision !== "merged_into_existing_topic_owner" || report.queue.filter((row) => row.clusterKey === "phase6t-dried-sweet-potato").every((row) => row.mergeTarget !== "phase6t-dried-fruit-basis"), "dried sweet-potato remains outside the dried-fruit topic owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "pickle-sodium")?.clusterDecision === "merged_into_existing_topic_owner", "legacy pickle-sodium cluster is merged into the researched candidate owner");
check(report.queue.filter((row) => row.clusterKey === "pickle-sodium").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "phase6t-pickle-basis"), "all legacy pickle-sodium rows merge into the researched phase6t candidate");
check(report.queue.filter((row) => /kimchi/.test(row.clusterKey)).every((row) => row.mergeTarget !== "phase6t-pickle-basis"), "kimchi clusters remain outside the pickle topic owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "canned-corn-sugar")?.clusterDecision === "deferred_pending_research", "canned-corn owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "canned-corn-sugar-basis")?.editorialDecision === "defer_research_needed", "canned-corn basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "canned-corn-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "canned-corn-sugar-basis"), "canned-corn siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cup-soup-sugar")?.clusterDecision === "deferred_pending_research", "cup-soup-sugar owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "cup-soup-sugar-basis")?.editorialDecision === "defer_research_needed", "cup-soup-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cup-soup-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cup-soup-sugar-basis"), "cup-soup-sugar siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "oatmeal-fiber")?.clusterDecision === "deferred_pending_research", "oatmeal-fiber owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "oatmeal-fiber-basis")?.editorialDecision === "defer_research_needed", "oatmeal-fiber basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "oatmeal-fiber" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "oatmeal-fiber-basis"), "oatmeal-fiber siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cold-noodle-sauce-sugar")?.clusterDecision === "deferred_pending_research", "cold-noodle-sauce-sugar owner cluster is parked behind its own research gate");
check(report.queue.find((row) => row.slug === "cold-noodle-sauce-sugar-basis")?.editorialDecision === "defer_research_needed", "cold-noodle-sauce-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cold-noodle-sauce-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cold-noodle-sauce-sugar-basis"), "cold-noodle-sauce-sugar siblings remain held behind their sugar-led owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "coffee-drink-sugar")?.clusterDecision === "deferred_pending_research", "coffee-drink-sugar owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "coffee-drink-sugar-basis")?.editorialDecision === "defer_research_needed", "coffee-drink-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "coffee-drink-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "coffee-drink-sugar-basis"), "coffee-drink-sugar siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "instant-oat-cup-sugar")?.clusterDecision === "deferred_pending_research", "instant-oat-cup-sugar remains a distinct cup-format owner behind a research gate");
check(report.queue.find((row) => row.slug === "instant-oat-cup-sugar-basis")?.editorialDecision === "defer_research_needed", "instant-oat-cup-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "instant-oat-cup-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "instant-oat-cup-sugar-basis"), "instant-oat-cup-sugar siblings remain held behind their cup-format owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "jam-sugar")?.clusterDecision === "deferred_pending_research", "jam-sugar owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "jam-sugar-basis")?.editorialDecision === "defer_research_needed", "jam-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "jam-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "jam-sugar-basis"), "jam-sugar siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cracker-sodium")?.clusterDecision === "deferred_pending_research", "cracker-sodium owner cluster is parked behind an explicit research gate");
check(report.queue.find((row) => row.slug === "cracker-sodium-basis")?.editorialDecision === "defer_research_needed", "cracker-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cracker-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cracker-sodium-basis"), "cracker-sodium siblings remain held behind the research-needed owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "jelly-drink-sugar")?.clusterDecision === "deferred_pending_research", "jelly-drink-sugar remains a distinct beverage/gel owner behind a research gate");
check(report.queue.find((row) => row.slug === "jelly-drink-sugar-basis")?.editorialDecision === "defer_research_needed", "jelly-drink-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "jelly-drink-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "jelly-drink-sugar-basis"), "jelly-drink-sugar siblings remain held behind their beverage/gel owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "sauce-sodium")?.clusterDecision === "deferred_pending_research", "generic sauce-sodium owner is parked behind a category-boundary research gate");
check(report.queue.find((row) => row.slug === "sauce-sodium-basis")?.editorialDecision === "defer_research_needed", "sauce-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "sauce-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "sauce-sodium-basis"), "sauce-sodium siblings remain held behind the generic table-sauce owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "kids-snack-sugar")?.clusterDecision === "deferred_pending_research", "kids-snack-sugar owner is parked behind an age and food-category research gate");
check(report.queue.find((row) => row.slug === "kids-snack-sugar-basis")?.editorialDecision === "defer_research_needed", "kids-snack-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "kids-snack-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "kids-snack-sugar-basis"), "kids-snack-sugar siblings remain held behind the child-snack owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "drink-per-100ml")?.clusterDecision === "deferred_pending_research", "drink-per-100ml owner is parked behind a beverage-category and denominator research gate");
check(report.queue.find((row) => row.slug === "drink-per-100ml-basis")?.editorialDecision === "defer_research_needed", "drink-per-100ml basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "drink-per-100ml" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "drink-per-100ml-basis"), "drink-per-100ml siblings remain held behind the educational normalization owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "salad-dressing-sugar")?.clusterDecision === "deferred_pending_research", "salad-dressing-sugar owner is parked behind a dressing-category and actual-use research gate");
check(report.queue.find((row) => row.slug === "salad-dressing-sugar-basis")?.editorialDecision === "defer_research_needed", "salad-dressing-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "salad-dressing-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "salad-dressing-sugar-basis"), "salad-dressing-sugar siblings remain held behind the researched dressing owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "powder-drink-sugar")?.clusterDecision === "deferred_pending_research", "powder-drink-sugar owner is parked behind a dry-versus-prepared research gate");
check(report.queue.find((row) => row.slug === "powder-drink-sugar-basis")?.editorialDecision === "defer_research_needed", "powder-drink-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "powder-drink-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "powder-drink-sugar-basis"), "powder-drink-sugar siblings remain held behind the powdered-drink owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "flavored-milk-sugar")?.clusterDecision === "deferred_pending_research", "flavored-milk-sugar owner is parked behind a dairy identity and current-label research gate");
check(report.queue.find((row) => row.slug === "flavored-milk-sugar-basis")?.editorialDecision === "defer_research_needed", "flavored-milk-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "flavored-milk-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "flavored-milk-sugar-basis"), "flavored-milk-sugar siblings remain held behind the processed-milk owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "dried-sweet-potato-sugar")?.clusterDecision === "deferred_pending_research", "dried-sweet-potato-sugar owner is parked behind a product and moisture-concentration research gate");
check(report.queue.find((row) => row.slug === "dried-sweet-potato-sugar-basis")?.editorialDecision === "defer_research_needed", "dried-sweet-potato-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "dried-sweet-potato-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "dried-sweet-potato-sugar-basis"), "dried-sweet-potato-sugar siblings remain held behind the dried-sweet-potato owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "protein-shake-sugar")?.clusterDecision === "deferred_pending_research", "protein-shake-sugar owner is parked behind a format, preparation and composition research gate");
check(report.queue.find((row) => row.slug === "protein-shake-sugar-basis")?.editorialDecision === "defer_research_needed", "protein-shake-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "protein-shake-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "protein-shake-sugar-basis"), "protein-shake-sugar siblings remain held behind the protein-shake owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "kids-drink-100ml")?.clusterDecision === "deferred_pending_research", "kids-drink-100ml owner is parked behind age, category and child-safety evidence gates");
check(report.queue.find((row) => row.slug === "kids-drink-100ml-basis")?.editorialDecision === "defer_research_needed", "kids-drink-100ml basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "kids-drink-100ml" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "kids-drink-100ml-basis"), "kids-drink-100ml siblings remain held behind the children's-drink owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cereal-sugar")?.clusterDecision === "deferred_pending_research", "cereal-sugar owner is parked behind category, dry-serving and milk-combination evidence gates");
check(report.queue.find((row) => row.slug === "cereal-sugar-basis")?.editorialDecision === "defer_research_needed", "cereal-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cereal-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cereal-sugar-basis"), "cereal-sugar siblings remain held behind the bowl-cereal owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "sports-drink-sugar")?.clusterDecision === "deferred_pending_research", "sports-drink-sugar owner is parked behind category, electrolyte and claim-evidence gates");
check(report.queue.find((row) => row.slug === "sports-drink-sugar-basis")?.editorialDecision === "defer_research_needed", "sports-drink-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "sports-drink-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "sports-drink-sugar-basis"), "sports-drink-sugar siblings remain held behind the sports-drink owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "coconut-water-sugar")?.clusterDecision === "deferred_pending_research", "coconut-water-sugar owner is parked behind product, electrolyte and claim-evidence gates");
check(report.queue.find((row) => row.slug === "coconut-water-sugar-basis")?.editorialDecision === "defer_research_needed", "coconut-water-sugar basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "coconut-water-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "coconut-water-sugar-basis"), "coconut-water-sugar siblings remain held behind the coconut-water owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "low-sugar-snack")?.clusterDecision === "deferred_pending_research", "low-sugar-snack owner is parked behind category, nutrition-claim and sweetener evidence gates");
check(report.queue.find((row) => row.slug === "low-sugar-snack-basis")?.editorialDecision === "defer_research_needed", "low-sugar-snack basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "low-sugar-snack" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "low-sugar-snack-basis"), "low-sugar-snack siblings remain held behind the low-sugar snack owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "ham-sausage-sodium")?.clusterDecision === "deferred_pending_research", "ham-sausage-sodium owner is parked behind product, ingredient and processed-meat claim-evidence gates");
check(report.queue.find((row) => row.slug === "ham-sausage-sodium-basis")?.editorialDecision === "defer_research_needed", "ham-sausage-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "ham-sausage-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "ham-sausage-sodium-basis"), "ham-sausage-sodium siblings remain held behind the ham-and-sausage owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "greek-yogurt-protein")?.clusterDecision === "deferred_pending_research", "greek-yogurt-protein owner is parked behind identity, composition and health-claim evidence gates");
check(report.queue.find((row) => row.slug === "greek-yogurt-protein-basis")?.editorialDecision === "defer_research_needed", "greek-yogurt-protein basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "greek-yogurt-protein" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "greek-yogurt-protein-basis"), "greek-yogurt-protein siblings remain held behind the Greek-yogurt owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "meal-replacement-drink")?.clusterDecision === "deferred_pending_research", "meal-replacement-drink owner is parked behind identity, completeness and health-claim evidence gates");
check(report.queue.find((row) => row.slug === "meal-replacement-drink-basis")?.editorialDecision === "defer_research_needed", "meal-replacement-drink basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "meal-replacement-drink" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "meal-replacement-drink-basis"), "meal-replacement-drink siblings remain held behind the meal-replacement owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "canned-tuna-sodium")?.clusterDecision === "deferred_pending_research", "canned-tuna-sodium owner is parked behind product, packing-medium and safety-claim evidence gates");
check(report.queue.find((row) => row.slug === "canned-tuna-sodium-basis")?.editorialDecision === "defer_research_needed", "canned-tuna-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "canned-tuna-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "canned-tuna-sodium-basis"), "canned-tuna-sodium siblings remain held behind the canned-tuna owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "tofu-snack-protein")?.clusterDecision === "deferred_pending_research", "tofu-snack-protein owner is parked behind product, formulation and claim-evidence gates");
check(report.queue.find((row) => row.slug === "tofu-snack-protein-basis")?.editorialDecision === "defer_research_needed", "tofu-snack-protein basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "tofu-snack-protein" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "tofu-snack-protein-basis"), "tofu-snack-protein siblings remain held behind the tofu-snack owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "energy-drink-label")?.clusterDecision === "merged_into_existing_topic_owner", "legacy energy-drink-label cluster is merged into the researched Phase 6T candidate");
check(report.queue.filter((row) => row.clusterKey === "energy-drink-label").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "phase6t-energy-drink-basis"), "all legacy energy-drink-label rows merge into the researched Phase 6T candidate");
check(report.clusters.find((cluster) => cluster.clusterKey === "granola-fat")?.clusterDecision === "deferred_pending_research", "granola-fat owner is parked behind category, dry-serving and composition evidence gates");
check(report.queue.find((row) => row.slug === "granola-fat-basis")?.editorialDecision === "defer_research_needed", "granola-fat basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "granola-fat" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "granola-fat-basis"), "granola-fat siblings remain held behind the granola owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "ready-side-dish-sodium")?.clusterDecision === "deferred_pending_research", "ready-side-dish-sodium owner is parked behind category, consumed-sauce and storage evidence gates");
check(report.queue.find((row) => row.slug === "ready-side-dish-sodium-basis")?.editorialDecision === "defer_research_needed", "ready-side-dish-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "ready-side-dish-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "ready-side-dish-sodium-basis"), "ready-side-dish-sodium siblings remain held behind the ready-side-dish owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "frozen-hotdog-satfat")?.clusterDecision === "deferred_pending_research", "frozen-hotdog-satfat owner is parked behind product-form, cooking-state and composition evidence gates");
check(report.queue.find((row) => row.slug === "frozen-hotdog-satfat-basis")?.editorialDecision === "defer_research_needed", "frozen-hotdog-satfat basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "frozen-hotdog-satfat" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "frozen-hotdog-satfat-basis"), "frozen-hotdog-satfat siblings remain held behind the frozen-hotdog owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "nutrition-serving-size")?.clusterDecision === "merged_into_existing_topic_owner", "nutrition-serving-size cluster is merged into the public nutrition-label comparison owner");
check(report.queue.filter((row) => row.clusterKey === "nutrition-serving-size").every((row) => row.editorialDecision === "reject_cannibalizing_sibling" && row.mergeTarget === "nutrition-label-comparison-basis"), "all nutrition-serving-size rows merge into the public nutrition-label comparison owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "instant-udon-sodium")?.clusterDecision === "deferred_pending_research", "instant-udon-sodium owner is parked behind product-form, component, preparation and broth-intake evidence gates");
check(report.queue.find((row) => row.slug === "instant-udon-sodium-basis")?.editorialDecision === "defer_research_needed", "instant-udon-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "instant-udon-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "instant-udon-sodium-basis"), "instant-udon-sodium siblings remain held behind the instant-udon owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "beef-jerky-sodium")?.clusterDecision === "deferred_pending_research", "beef-jerky-sodium owner is parked behind product, drying, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "beef-jerky-sodium-basis")?.editorialDecision === "defer_research_needed", "beef-jerky-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "beef-jerky-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "beef-jerky-sodium-basis"), "beef-jerky-sodium siblings remain held behind the beef-jerky owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "rice-cake-snack-carbs")?.clusterDecision === "deferred_pending_research", "rice-cake-snack-carbs owner is parked behind product, filling, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "rice-cake-snack-carbs-basis")?.editorialDecision === "defer_research_needed", "rice-cake-snack-carbs basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "rice-cake-snack-carbs" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "rice-cake-snack-carbs-basis"), "rice-cake-snack-carbs siblings remain held behind the rice-cake snack owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "dumpling-sodium-calorie")?.clusterDecision === "deferred_pending_research", "dumpling-sodium-calorie owner is parked behind type, cooking-state, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "dumpling-sodium-calorie-basis")?.editorialDecision === "defer_research_needed", "dumpling-sodium-calorie basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "dumpling-sodium-calorie" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "dumpling-sodium-calorie-basis"), "dumpling-sodium-calorie siblings remain held behind the dumpling owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "porridge-soup-sodium")?.clusterDecision === "deferred_pending_research", "porridge-soup-sodium owner is parked behind scope, preparation, consumption and storage evidence gates");
check(report.queue.find((row) => row.slug === "porridge-soup-sodium-basis")?.editorialDecision === "defer_research_needed", "porridge-soup-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "porridge-soup-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "porridge-soup-sodium-basis"), "porridge-soup-sodium siblings remain held behind the porridge/soup owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "seaweed-snack-sodium")?.clusterDecision === "deferred_pending_research", "seaweed-snack-sodium owner is parked behind product, portion, seasoning and storage evidence gates");
check(report.queue.find((row) => row.slug === "seaweed-snack-sodium-basis")?.editorialDecision === "defer_research_needed", "seaweed-snack-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "seaweed-snack-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "seaweed-snack-sodium-basis"), "seaweed-snack-sodium siblings remain held behind the seaweed-snack owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cheese-protein-fat")?.clusterDecision === "deferred_pending_research", "cheese-protein-fat owner is parked behind type, classification, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "cheese-protein-fat-basis")?.editorialDecision === "defer_research_needed", "cheese-protein-fat basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cheese-protein-fat" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cheese-protein-fat-basis"), "cheese-protein-fat siblings remain held behind the cheese owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "gimbap-line-nutrition")?.clusterDecision === "deferred_pending_research", "gimbap-line-nutrition owner is parked behind composition, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "gimbap-line-nutrition-basis")?.editorialDecision === "defer_research_needed", "gimbap-line-nutrition basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "gimbap-line-nutrition" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "gimbap-line-nutrition-basis"), "gimbap-line-nutrition siblings remain held behind the whole-roll gimbap owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "fishcake-soup-mealkit")?.clusterDecision === "deferred_pending_research", "fishcake-soup-mealkit owner is parked behind component, preparation, broth-intake and storage evidence gates");
check(report.queue.find((row) => row.slug === "fishcake-soup-mealkit-basis")?.editorialDecision === "defer_research_needed", "fishcake-soup-mealkit basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "fishcake-soup-mealkit" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "fishcake-soup-mealkit-basis"), "fishcake-soup-mealkit siblings remain held behind the fishcake-soup owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "fried-rice-meal-sodium")?.clusterDecision === "deferred_pending_research", "fried-rice-meal-sodium owner is parked behind product, cooking-state, add-in and storage evidence gates");
check(report.queue.find((row) => row.slug === "fried-rice-meal-sodium-basis")?.editorialDecision === "defer_research_needed", "fried-rice-meal-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "fried-rice-meal-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "fried-rice-meal-sodium-basis"), "fried-rice-meal-sodium siblings remain held behind the fried-rice owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "lunchbox-calorie")?.clusterDecision === "deferred_pending_research", "lunchbox-calorie owner is parked behind product, composition, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "lunchbox-calorie-basis")?.editorialDecision === "defer_research_needed", "lunchbox-calorie basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "lunchbox-calorie" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "lunchbox-calorie-basis"), "lunchbox-calorie siblings remain held behind the lunchbox owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "frozen-pizza-label")?.clusterDecision === "deferred_pending_research", "frozen-pizza-label owner is parked behind product, portion, preparation and storage evidence gates");
check(report.queue.find((row) => row.slug === "frozen-pizza-label-basis")?.editorialDecision === "defer_research_needed", "frozen-pizza-label basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "frozen-pizza-label" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "frozen-pizza-label-basis"), "frozen-pizza-label siblings remain held behind the frozen-pizza owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cup-noodle-sodium")?.clusterDecision === "deferred_pending_research", "cup-noodle-sodium owner is parked behind product, preparation, broth-intake and storage evidence gates");
check(report.queue.find((row) => row.slug === "cup-noodle-sodium-basis")?.editorialDecision === "defer_research_needed", "cup-noodle-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cup-noodle-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cup-noodle-sodium-basis"), "cup-noodle-sodium siblings remain held behind the cup-noodle owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "frozen-food-sodium")?.clusterDecision === "deferred_pending_research", "frozen-food-sodium owner is parked behind subclass, component, preparation and storage evidence gates");
check(report.queue.find((row) => row.slug === "frozen-food-sodium-basis")?.editorialDecision === "defer_research_needed", "frozen-food-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "frozen-food-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "frozen-food-sodium-basis"), "frozen-food-sodium siblings remain held behind the frozen-food owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "triangle-kimbap-calorie")?.clusterDecision === "deferred_pending_research", "triangle-kimbap-calorie owner is parked behind product, composition, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "triangle-kimbap-calorie-basis")?.editorialDecision === "defer_research_needed", "triangle-kimbap-calorie basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "triangle-kimbap-calorie" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "triangle-kimbap-calorie-basis"), "triangle-kimbap-calorie siblings remain held behind the triangle-kimbap owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "soup-base-sodium")?.clusterDecision === "deferred_pending_research", "soup-base-sodium owner is parked behind form, dilution, broth-intake and storage evidence gates");
check(report.queue.find((row) => row.slug === "soup-base-sodium-basis")?.editorialDecision === "defer_research_needed", "soup-base-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "soup-base-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "soup-base-sodium-basis"), "soup-base-sodium siblings remain held behind the soup-base owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "kimchi-sodium-label")?.clusterDecision === "deferred_pending_research", "kimchi-sodium-label owner is parked behind type, solids/brine, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "kimchi-sodium-label-basis")?.editorialDecision === "defer_research_needed", "kimchi-sodium-label basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "kimchi-sodium-label" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "kimchi-sodium-label-basis"), "kimchi-sodium-label siblings remain held behind the kimchi owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "pasta-sauce-sodium")?.clusterDecision === "deferred_pending_research", "pasta-sauce-sodium owner is parked behind style, preparation, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "pasta-sauce-sodium-basis")?.editorialDecision === "defer_research_needed", "pasta-sauce-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "pasta-sauce-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "pasta-sauce-sodium-basis"), "pasta-sauce-sodium siblings remain held behind the pasta-sauce owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "chicken-sausage-sodium")?.clusterDecision === "deferred_pending_research", "chicken-sausage-sodium owner is parked behind composition, cooking-state, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "chicken-sausage-sodium-basis")?.editorialDecision === "defer_research_needed", "chicken-sausage-sodium basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "chicken-sausage-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "chicken-sausage-sodium-basis"), "chicken-sausage-sodium siblings remain held behind the chicken-sausage owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "zero-sugar-drink")?.clusterDecision === "deferred_pending_research", "zero-sugar-drink owner is parked behind claim, drink-class, denominator and ingredient evidence gates");
check(report.queue.find((row) => row.slug === "zero-sugar-drink-basis")?.editorialDecision === "defer_research_needed", "zero-sugar-drink basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "zero-sugar-drink" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "zero-sugar-drink-basis"), "zero-sugar-drink siblings remain held behind the zero-sugar owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "tteokbokki-mealkit-sodium")?.clusterDecision === "deferred_pending_research", "tteokbokki meal-kit owner is parked behind component, preparation, consumption and denominator evidence gates");
check(report.queue.find((row) => row.slug === "tteokbokki-mealkit-sodium-basis")?.editorialDecision === "defer_research_needed", "tteokbokki meal-kit basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "tteokbokki-mealkit-sodium" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "tteokbokki-mealkit-sodium-basis"), "tteokbokki meal-kit siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "convenience-protein")?.clusterDecision === "deferred_pending_research", "convenience-protein owner is parked behind food-class, source, preparation and denominator evidence gates");
check(report.queue.find((row) => row.slug === "convenience-protein-basis")?.editorialDecision === "defer_research_needed", "convenience-protein basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "convenience-protein" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "convenience-protein-basis"), "convenience-protein siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "soy-milk-sugar")?.clusterDecision === "deferred_pending_research", "soy-milk owner is parked behind classification, soy composition, claim and denominator evidence gates");
check(report.queue.find((row) => row.slug === "soy-milk-sugar-basis")?.editorialDecision === "defer_research_needed", "soy-milk basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "soy-milk-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "soy-milk-sugar-basis"), "soy-milk siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "cereal-milk-combo")?.clusterDecision === "deferred_pending_research", "cereal-milk combination owner is parked behind component, preparation, consumption and denominator evidence gates");
check(report.queue.find((row) => row.slug === "cereal-milk-combo-basis")?.editorialDecision === "defer_research_needed", "cereal-milk combination basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "cereal-milk-combo" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "cereal-milk-combo-basis"), "cereal-milk combination siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "per-100kcal-protein")?.clusterDecision === "deferred_pending_research", "protein-per-100-kcal owner is parked behind population, state, source and calculation evidence gates");
check(report.queue.find((row) => row.slug === "per-100kcal-protein-basis")?.editorialDecision === "defer_research_needed", "protein-per-100-kcal basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "per-100kcal-protein" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "per-100kcal-protein-basis"), "protein-per-100-kcal siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "nutrition-claim-wording")?.clusterDecision === "deferred_pending_research", "nutrition-claim wording owner is parked behind jurisdiction, rule, threshold and evidence-source gates");
check(report.queue.find((row) => row.slug === "nutrition-claim-wording-basis")?.editorialDecision === "defer_research_needed", "nutrition-claim wording basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "nutrition-claim-wording" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "nutrition-claim-wording-basis"), "nutrition-claim wording siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "salad-lunchbox-dressing")?.clusterDecision === "deferred_pending_research", "salad-lunchbox owner is parked behind component, dressing-use, consumption and cold-chain evidence gates");
check(report.queue.find((row) => row.slug === "salad-lunchbox-dressing-basis")?.editorialDecision === "defer_research_needed", "salad-lunchbox basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "salad-lunchbox-dressing" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "salad-lunchbox-dressing-basis"), "salad-lunchbox siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "instant-curry-calorie")?.clusterDecision === "deferred_pending_research", "instant-curry owner is parked behind category, preparation, component-consumption and storage evidence gates");
check(report.queue.find((row) => row.slug === "instant-curry-calorie-basis")?.editorialDecision === "defer_research_needed", "instant-curry basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "instant-curry-calorie" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "instant-curry-calorie-basis"), "instant-curry siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "no-sugar-label")?.clusterDecision === "deferred_pending_research", "no-sugar label owner is parked behind jurisdiction, wording, threshold and sugar-source evidence gates");
check(report.queue.find((row) => row.slug === "no-sugar-label-basis")?.editorialDecision === "defer_research_needed", "no-sugar label basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "no-sugar-label" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "no-sugar-label-basis"), "no-sugar label siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "bread-sugar")?.clusterDecision === "deferred_pending_research", "bread owner is parked behind product-class, composition, preparation and denominator evidence gates");
check(report.queue.find((row) => row.slug === "bread-sugar-basis")?.editorialDecision === "defer_research_needed", "bread basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "bread-sugar" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "bread-sugar-basis"), "bread siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "instant-rice-carbs")?.clusterDecision === "deferred_pending_research", "instant-rice owner is parked behind rice-class, preparation, denominator and storage evidence gates");
check(report.queue.find((row) => row.slug === "instant-rice-carbs-basis")?.editorialDecision === "defer_research_needed", "instant-rice basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "instant-rice-carbs" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "instant-rice-carbs-basis"), "instant-rice siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "nuts-serving-size")?.clusterDecision === "deferred_pending_research", "nuts-serving-size owner is parked behind species, processing, edible-weight and allergen evidence gates");
check(report.queue.find((row) => row.slug === "nuts-serving-size-basis")?.editorialDecision === "defer_research_needed", "nuts-serving-size basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "nuts-serving-size" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "nuts-serving-size-basis"), "nuts-serving-size siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "mini-snack-total-amount")?.clusterDecision === "deferred_pending_research", "mini-snack owner is parked behind class, multipack, unit-count and actual-intake evidence gates");
check(report.queue.find((row) => row.slug === "mini-snack-total-amount-basis")?.editorialDecision === "defer_research_needed", "mini-snack basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "mini-snack-total-amount" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "mini-snack-total-amount-basis"), "mini-snack siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "data-update-date")?.clusterDecision === "deferred_pending_research", "data-update-date owner is parked behind source, version, date-semantics and stale-data evidence gates");
check(report.queue.find((row) => row.slug === "data-update-date-basis")?.editorialDecision === "defer_research_needed", "data-update-date basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "data-update-date" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "data-update-date-basis"), "data-update-date siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "icecream-satfat")?.clusterDecision === "deferred_pending_research", "ice-cream owner is parked behind frozen-dessert class, format, fat-source and cold-chain evidence gates");
check(report.queue.find((row) => row.slug === "icecream-satfat-basis")?.editorialDecision === "defer_research_needed", "ice-cream basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "icecream-satfat" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "icecream-satfat-basis"), "ice-cream siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "food-category-code")?.clusterDecision === "deferred_pending_research", "food-category-code owner is parked behind authority, version, hierarchy and mapping-provenance evidence gates");
check(report.queue.find((row) => row.slug === "food-category-code-basis")?.editorialDecision === "defer_research_needed", "food-category-code basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "food-category-code" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "food-category-code-basis"), "food-category-code siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "per-100g-food")?.clusterDecision === "deferred_pending_research", "per-100-g owner is parked behind population, food-state, source and conversion evidence gates");
check(report.queue.find((row) => row.slug === "per-100g-food-basis")?.editorialDecision === "defer_research_needed", "per-100-g basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "per-100g-food" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "per-100g-food-basis"), "per-100-g siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "ingredient-list-reading")?.clusterDecision === "deferred_pending_research", "ingredient-list owner is parked behind jurisdiction, label-version, compound-ingredient and allergen evidence gates");
check(report.queue.find((row) => row.slug === "ingredient-list-reading-basis")?.editorialDecision === "defer_research_needed", "ingredient-list basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "ingredient-list-reading" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "ingredient-list-reading-basis"), "ingredient-list siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "noodle-topping-calorie")?.clusterDecision === "deferred_pending_research", "noodle-topping owner is parked behind component, preparation, actual-consumption and storage evidence gates");
check(report.queue.find((row) => row.slug === "noodle-topping-calorie-basis")?.editorialDecision === "defer_research_needed", "noodle-topping basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "noodle-topping-calorie" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "noodle-topping-calorie-basis"), "noodle-topping siblings remain held behind the owner");
check(report.clusters.find((cluster) => cluster.clusterKey === "public-data-nutrition")?.clusterDecision === "deferred_pending_research", "public-data nutrition owner is parked behind dataset identity, coverage, transformation and operations evidence gates");
check(report.queue.find((row) => row.slug === "public-data-nutrition-basis")?.editorialDecision === "defer_research_needed", "public-data nutrition basis is not returned to drafting priority");
check(report.queue.filter((row) => row.clusterKey === "public-data-nutrition" && row.variant !== "basis").every((row) => row.editorialDecision === "hold_for_research_needed" && row.mergeTarget === "public-data-nutrition-basis"), "public-data nutrition siblings remain held behind the owner");
check(report.queue.every((row) => row.humanReview === "pending"), "all legacy rows remain pending in the planning queue");
check(report.queue.every((row) => row.editorialDecision !== "approved"), "planning queue never impersonates human approval");
check(report.queue.every((row) => row.sourceCount === 3 && row.internalLinkCount === 3), "queue records the shared three-source and three-link legacy shape");
check(report.queue.every((row) => row.researchPresent === false && row.qaEvidencePresent === false), "legacy queue records missing per-article research and QA evidence");
check(report.queue.every((row) => row.contractShapeSignature.length === 64 && row.semanticContentSignature.length === 64), "queue records deterministic contract and semantic hashes");
check(report.clusters.filter((cluster) => cluster.candidate).filter((cluster) => cluster.candidate.readyForHumanReview).length === 7, "all seven recognized candidates are ready for human review");
check(report.clusters.filter((cluster) => cluster.candidate).every((cluster) => cluster.candidate.status === "approval_candidate_pending_human_review"), "all recognized candidates retain pending-human-review status");
check(report.clusters.filter((cluster) => cluster.candidate).every((cluster) => cluster.candidate.packetFilesPresent && cluster.candidate.researchSourceCount >= 5), "recognized candidates have complete packet files and at least five recorded research sources");
check(script.includes("generatedFilePattern"), "mapper limits discovery to generated phase corpus files");
check(script.includes('editorialDecision = "hold_for_cluster_contract_review"'), "mapper fails closed for unreviewed sibling rows");
check(script.includes("readExplicitDecisions"), "mapper consumes explicit cross-cluster decisions");
check(script.includes("Do not draft from held rows"), "mapper records the anti-template continuation rule");

console.log(`editorial corpus map: ${assertions.length} assertions passed`);
