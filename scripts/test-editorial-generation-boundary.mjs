import { readFileSync } from "node:fs";
import path from "node:path";

const root = process.cwd();
const assertions = [];

function check(condition, message) {
  if (!condition) throw new Error(message);
  assertions.push(message);
}

const blogSource = readFileSync(path.join(root, "lib", "blog.ts"), "utf8");
const legacyGenerator = readFileSync(path.join(root, "scripts", "generate-scheduled-blog-batch.mjs"), "utf8");
const phase6tGenerator = readFileSync(path.join(root, "scripts", "generate-phase6t-fourmonth-blog.mjs"), "utf8");
const readinessReview = readFileSync(path.join(root, "scripts", "review-blog-editorial-readiness.mjs"), "utf8");
const candidateRoot = path.join(root, "content", "editorial-candidates", "phase6t-energy-drink-basis");
const candidateContract = JSON.parse(readFileSync(path.join(candidateRoot, "contract.json"), "utf8"));
const candidateResearch = JSON.parse(readFileSync(path.join(candidateRoot, "research.json"), "utf8"));
const candidateQa = JSON.parse(readFileSync(path.join(candidateRoot, "qa.json"), "utf8"));
const candidateDraft = readFileSync(path.join(candidateRoot, "draft.mdx"), "utf8");
const candidateBody = candidateDraft.replace(/^---[\s\S]*?---\s*/, "");
const driedFruitRoot = path.join(root, "content", "editorial-candidates", "phase6t-dried-fruit-basis");
const driedFruitContract = JSON.parse(readFileSync(path.join(driedFruitRoot, "contract.json"), "utf8"));
const driedFruitResearch = JSON.parse(readFileSync(path.join(driedFruitRoot, "research.json"), "utf8"));
const driedFruitQa = JSON.parse(readFileSync(path.join(driedFruitRoot, "qa.json"), "utf8"));
const driedFruitDraft = readFileSync(path.join(driedFruitRoot, "draft.mdx"), "utf8");
const driedFruitBody = driedFruitDraft.replace(/^---[\s\S]*?---\s*/, "");
const saladToppingRoot = path.join(root, "content", "editorial-candidates", "phase6t-salad-topping-basis");
const saladToppingContract = JSON.parse(readFileSync(path.join(saladToppingRoot, "contract.json"), "utf8"));
const saladToppingResearch = JSON.parse(readFileSync(path.join(saladToppingRoot, "research.json"), "utf8"));
const saladToppingQa = JSON.parse(readFileSync(path.join(saladToppingRoot, "qa.json"), "utf8"));
const saladToppingDraft = readFileSync(path.join(saladToppingRoot, "draft.mdx"), "utf8");
const saladToppingBody = saladToppingDraft.replace(/^---[\s\S]*?---\s*/, "");
const konjacJellyRoot = path.join(root, "content", "editorial-candidates", "phase6t-konjac-jelly-basis");
const konjacJellyContract = JSON.parse(readFileSync(path.join(konjacJellyRoot, "contract.json"), "utf8"));
const konjacJellyResearch = JSON.parse(readFileSync(path.join(konjacJellyRoot, "research.json"), "utf8"));
const konjacJellyQa = JSON.parse(readFileSync(path.join(konjacJellyRoot, "qa.json"), "utf8"));
const konjacJellyDraft = readFileSync(path.join(konjacJellyRoot, "draft.mdx"), "utf8");
const konjacJellyBody = konjacJellyDraft.replace(/^---[\s\S]*?---\s*/, "");
const bagelRoot = path.join(root, "content", "editorial-candidates", "phase6t-bagel-basis");
const bagelContract = JSON.parse(readFileSync(path.join(bagelRoot, "contract.json"), "utf8"));
const bagelResearch = JSON.parse(readFileSync(path.join(bagelRoot, "research.json"), "utf8"));
const bagelQa = JSON.parse(readFileSync(path.join(bagelRoot, "qa.json"), "utf8"));
const bagelDraft = readFileSync(path.join(bagelRoot, "draft.mdx"), "utf8");
const bagelBody = bagelDraft.replace(/^---[\s\S]*?---\s*/, "");
const pickleRoot = path.join(root, "content", "editorial-candidates", "phase6t-pickle-basis");
const pickleContract = JSON.parse(readFileSync(path.join(pickleRoot, "contract.json"), "utf8"));
const pickleResearch = JSON.parse(readFileSync(path.join(pickleRoot, "research.json"), "utf8"));
const pickleQa = JSON.parse(readFileSync(path.join(pickleRoot, "qa.json"), "utf8"));
const pickleDraft = readFileSync(path.join(pickleRoot, "draft.mdx"), "utf8");
const pickleBody = pickleDraft.replace(/^---[\s\S]*?---\s*/, "");
const editorialDecisions = JSON.parse(readFileSync(path.join(root, "content", "editorial-candidates", "decisions.json"), "utf8"));
const bakeryRoot = path.join(root, "content", "editorial-candidates", "bakery-snack-serving-basis");
const bakeryContract = JSON.parse(readFileSync(path.join(bakeryRoot, "contract.json"), "utf8"));
const bakeryResearch = JSON.parse(readFileSync(path.join(bakeryRoot, "research.json"), "utf8"));
const bakeryQa = JSON.parse(readFileSync(path.join(bakeryRoot, "qa.json"), "utf8"));
const bakeryDraft = readFileSync(path.join(bakeryRoot, "draft.mdx"), "utf8");
const bakeryBody = bakeryDraft.replace(/^---[\s\S]*?---\s*/, "");

check(blogSource.includes('humanReview: post.humanReview || "pending"'), "generated posts default to pending review");
check(blogSource.includes('noindex: post.humanReview === "approved" ? post.noindex : true'), "unapproved generated posts default to noindex");
check(blogSource.includes('post.humanReview !== "pending"'), "pending posts are excluded from public post queries");
check(blogSource.includes("function keepUniqueSlugs(posts: BlogPost[])"), "public blog lists fail closed on duplicate eligible slugs");
check(blogSource.includes("if (matches.length !== 1)"), "public blog details fail closed on duplicate eligible slugs");
check(blogSource.includes("표시된 영양성분 값에 목표 섭취량을 곱한 뒤 표시 기준량으로 나누는 방식"), "public normalization owner states the reusable serving conversion formula");
check(blogSource.includes("동일한 식품군과 사용 목적 안에서만 비교"), "public normalization owner rejects cross-category ranking");
check(legacyGenerator.includes("Legacy template generation is disabled"), "legacy scheduled template generator is disabled");
check(phase6tGenerator.includes("Legacy template generation is disabled"), "phase6t template generator is disabled");
check(readinessReview.includes("missing_per_article_research_packet"), "readiness review requires per-article research");
check(readinessReview.includes("unsupported_numeric_quality_score"), "readiness review rejects unsupported numeric scores");
check(readinessReview.includes("high_frequency_heading_template"), "readiness review detects repeated heading templates");
check(readinessReview.includes("known_korean_language_defect"), "readiness review detects known Korean language defects");
check(readinessReview.includes('decision: issues.length === 0 ? "approval_candidate" : "review_needed"'), "approval candidates require zero detected issues");
check(candidateContract.status === "accepted_for_draft", "candidate has an accepted article contract");
check(candidateResearch.research_runs.length >= 3 && candidateResearch.research_runs.length <= 5, "candidate records 3-5 research runs");
check(candidateResearch.sources.length >= 5 && candidateResearch.sources.length <= 8, "candidate records 5-8 sources");
check(candidateResearch.sources.filter((source) => source.is_official).length >= 2, "YMYL candidate has at least two official sources");
check(candidateResearch.fact_traceability_pass === true && candidateResearch.unresolved_claims.length === 0, "candidate fact traceability passes without unresolved claims");
check(candidateResearch.article_specific_details.length >= 2, "candidate has article-specific source details");
check(candidateBody.length >= 3500, "candidate body exceeds 3500 characters");
check((candidateBody.match(/^## /gm) || []).length >= 4, "candidate uses specific H2 sections");
check((candidateBody.match(/\]\(\//g) || []).length >= 3, "candidate includes at least three internal links");
check(!/당류을|에너지음료은|사람라면|기준 읽기을/.test(candidateBody), "candidate removes known Korean particle defects");
check(candidateDraft.includes('humanReview: "pending"') && candidateDraft.includes("noindex: true"), "candidate remains pending and noindex");
check(blogSource.includes('path.join(process.cwd(), "content", "blog")'), "public loader is restricted to content/blog and ignores editorial candidates");
check(candidateQa.score >= 90 && Object.values(candidateQa.hard_gate_results).every((result) => result === "pass"), "energy-drink candidate QA score and hard gates pass");
check(editorialDecisions.decisions.length === 374, "seven candidate clusters, sixty-one cross-cluster reviews and eighty research defers have explicit editorial decisions");
check(editorialDecisions.decisions.filter((item) => item.legacyDecision === "reject_cannibalizing_sibling").length === 287, "two hundred eighty-seven explicitly reviewed cannibalizing rows are rejected");
check(editorialDecisions.decisions.find((item) => item.slug === "fried-rice-meal-sodium-basis")?.legacyDecision === "defer_research_needed", "fried-rice owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "fried-rice-meal-sodium-basis")?.reason.includes("required or optional oil/additions"), "fried-rice research gate preserves cooking oil and additions");
check(editorialDecisions.decisions.find((item) => item.slug === "lunchbox-calorie-basis")?.legacyDecision === "defer_research_needed", "lunchbox owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "lunchbox-calorie-basis")?.reason.includes("separate rice/noodle/protein main and side-dish/sauce weights"), "lunchbox research gate preserves main, side-dish and sauce composition");
check(editorialDecisions.decisions.find((item) => item.slug === "frozen-pizza-label-basis")?.legacyDecision === "defer_research_needed", "frozen-pizza owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "frozen-pizza-label-basis")?.reason.includes("pizza style/diameter/whole weight/slice-count strata"), "frozen-pizza research gate preserves pizza size, whole-weight and slice-count identity");
check(editorialDecisions.decisions.find((item) => item.slug === "cup-noodle-sodium-basis")?.legacyDecision === "defer_research_needed", "cup-noodle owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "cup-noodle-sodium-basis")?.reason.includes("solids-eaten versus broth-consumed"), "cup-noodle research gate preserves solid and broth consumption separation");
check(editorialDecisions.decisions.find((item) => item.slug === "frozen-food-sodium-basis")?.legacyDecision === "defer_research_needed", "frozen-food owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "frozen-food-sodium-basis")?.reason.includes("defensible single subclass or explicitly stratified population"), "frozen-food research gate prevents incompatible subclasses from being pooled");
check(editorialDecisions.decisions.find((item) => item.slug === "triangle-kimbap-calorie-basis")?.legacyDecision === "defer_research_needed", "triangle-kimbap owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "triangle-kimbap-calorie-basis")?.reason.includes("separate rice/filling/seaweed/sauce/oil composition"), "triangle-kimbap research gate preserves rice, filling, seaweed, sauce and oil composition");
check(editorialDecisions.decisions.find((item) => item.slug === "soup-base-sodium-basis")?.legacyDecision === "defer_research_needed", "soup-base owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "soup-base-sodium-basis")?.reason.includes("exact dilution ratio/water/additions and prepared yield"), "soup-base research gate preserves dilution and prepared yield");
check(editorialDecisions.decisions.find((item) => item.slug === "kimchi-sodium-label-basis")?.legacyDecision === "defer_research_needed", "kimchi owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "kimchi-sodium-label-basis")?.reason.includes("net/drained/undrained weight and separate solids/brine/seasoning treatment"), "kimchi research gate preserves drained state, solids, brine and seasoning");
check(editorialDecisions.decisions.find((item) => item.slug === "pasta-sauce-sodium-basis")?.legacyDecision === "defer_research_needed", "pasta-sauce owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "pasta-sauce-sodium-basis")?.reason.includes("pasta amount and prepared-dish yield"), "pasta-sauce research gate preserves sauce-only and prepared-dish denominators");
check(editorialDecisions.decisions.find((item) => item.slug === "chicken-sausage-sodium-basis")?.legacyDecision === "defer_research_needed", "chicken-sausage owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "chicken-sausage-sodium-basis")?.reason.includes("poultry/meat species and percentage population"), "chicken-sausage research gate preserves meat species and content identity");
check(editorialDecisions.decisions.find((item) => item.slug === "zero-sugar-drink-basis")?.legacyDecision === "defer_research_needed", "zero-sugar-drink owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "zero-sugar-drink-basis")?.reason.includes("applicable jurisdiction, exact zero/no-sugar claim wording and current label date"), "zero-sugar-drink research gate preserves claim jurisdiction and label currency");
check(editorialDecisions.decisions.find((item) => item.slug === "tteokbokki-mealkit-sodium-basis")?.legacyDecision === "defer_research_needed", "tteokbokki meal-kit owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "tteokbokki-mealkit-sodium-basis")?.reason.includes("solids/sauce/broth-consumed states"), "tteokbokki meal-kit research gate preserves actual component consumption");
check(editorialDecisions.decisions.find((item) => item.slug === "convenience-protein-basis")?.legacyDecision === "defer_research_needed", "convenience-protein owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "convenience-protein-basis")?.reason.includes("animal/plant protein source and percentage"), "convenience-protein research gate preserves protein source and composition");
check(editorialDecisions.decisions.find((item) => item.slug === "soy-milk-sugar-basis")?.legacyDecision === "defer_research_needed", "soy-milk owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "soy-milk-sugar-basis")?.reason.includes("whole-soybean/soy-solids/soy-protein/isolate composition and percentages"), "soy-milk research gate preserves soy composition identity");
check(editorialDecisions.decisions.find((item) => item.slug === "cereal-milk-combo-basis")?.legacyDecision === "defer_research_needed", "cereal-milk combination owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "cereal-milk-combo-basis")?.reason.includes("actual cereal/liquid consumed or left over"), "cereal-milk combination research gate preserves actual component consumption");
check(editorialDecisions.decisions.find((item) => item.slug === "per-100kcal-protein-basis")?.legacyDecision === "defer_research_needed", "protein-per-100-kcal owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "per-100kcal-protein-basis")?.reason.includes("explicit protein-per-100-kcal formula with rounding plus missing or zero-energy handling"), "protein-per-100-kcal research gate preserves reproducible calculation rules");
check(editorialDecisions.decisions.find((item) => item.slug === "nutrition-claim-wording-basis")?.legacyDecision === "defer_research_needed", "nutrition-claim wording owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "nutrition-claim-wording-basis")?.reason.includes("regulation/version and effective date"), "nutrition-claim wording research gate preserves jurisdictional rule currency");
check(editorialDecisions.decisions.find((item) => item.slug === "salad-lunchbox-dressing-basis")?.legacyDecision === "defer_research_needed", "salad-lunchbox owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "salad-lunchbox-dressing-basis")?.reason.includes("included-packet versus amount-actually-used distinction"), "salad-lunchbox research gate preserves actual dressing consumption");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-curry-calorie-basis")?.legacyDecision === "defer_research_needed", "instant-curry owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-curry-calorie-basis")?.reason.includes("sauce/solids/rice actually consumed or left over"), "instant-curry research gate preserves component-level actual consumption");
check(editorialDecisions.decisions.find((item) => item.slug === "no-sugar-label-basis")?.legacyDecision === "defer_research_needed", "no-sugar label owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "no-sugar-label-basis")?.reason.includes("naturally occurring versus added and total sugar distinction"), "no-sugar label research gate preserves sugar-source distinctions");
check(editorialDecisions.decisions.find((item) => item.slug === "bread-sugar-basis")?.legacyDecision === "defer_research_needed", "bread owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "bread-sugar-basis")?.reason.includes("plain-sliced/roll/sweet-bread/pastry/filled/topped/coated product population"), "bread research gate preserves product-class boundaries");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-rice-carbs-basis")?.legacyDecision === "defer_research_needed", "instant-rice owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-rice-carbs-basis")?.reason.includes("white/brown/multigrain/konjac/seasoned rice population"), "instant-rice research gate preserves rice-class and composition boundaries");
check(editorialDecisions.decisions.find((item) => item.slug === "nuts-serving-size-basis")?.legacyDecision === "defer_research_needed", "nuts-serving-size owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "nuts-serving-size-basis")?.reason.includes("single-nut or mixed-nut population with species and disclosed mixture percentages"), "nuts-serving-size research gate preserves species and mixture composition");
check(editorialDecisions.decisions.find((item) => item.slug === "mini-snack-total-amount-basis")?.legacyDecision === "defer_research_needed", "mini-snack owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "mini-snack-total-amount-basis")?.reason.includes("outer multipack versus individual-pack identity"), "mini-snack research gate preserves multipack and individual-unit identity");
check(editorialDecisions.decisions.find((item) => item.slug === "data-update-date-basis")?.legacyDecision === "defer_research_needed", "data-update-date owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "data-update-date-basis")?.reason.includes("observation, retrieval, publication, revision, effective and expiration dates"), "data-update-date research gate preserves distinct date semantics");
check(editorialDecisions.decisions.find((item) => item.slug === "icecream-satfat-basis")?.legacyDecision === "defer_research_needed", "ice-cream owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "icecream-satfat-basis")?.reason.includes("ice-cream/ice-milk/gelato/sorbet/frozen-yogurt/plant-based-frozen-dessert class"), "ice-cream research gate preserves frozen-dessert class boundaries");
check(editorialDecisions.decisions.find((item) => item.slug === "food-category-code-basis")?.legacyDecision === "defer_research_needed", "food-category-code owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "food-category-code-basis")?.reason.includes("record-to-code provenance with mapping method and date"), "food-category-code research gate preserves record-level mapping provenance");
check(editorialDecisions.decisions.find((item) => item.slug === "per-100g-food-basis")?.legacyDecision === "defer_research_needed", "per-100-g owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "per-100g-food-basis")?.reason.includes("explicit per-100-g conversion formula with units, rounding and missing or zero-weight handling"), "per-100-g research gate preserves reproducible conversion rules");
check(editorialDecisions.decisions.find((item) => item.slug === "ingredient-list-reading-basis")?.legacyDecision === "defer_research_needed", "ingredient-list owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "ingredient-list-reading-basis")?.reason.includes("absent versus not-disclosed versus zero distinctions"), "ingredient-list research gate preserves missing-evidence semantics");
check(editorialDecisions.decisions.find((item) => item.slug === "noodle-topping-calorie-basis")?.legacyDecision === "defer_research_needed", "noodle-topping owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "noodle-topping-calorie-basis")?.reason.includes("solids/sauce/broth/toppings actually consumed or left over"), "noodle-topping research gate preserves component-level actual consumption");
check(editorialDecisions.decisions.find((item) => item.slug === "public-data-nutrition-basis")?.legacyDecision === "defer_research_needed", "public-data nutrition owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "public-data-nutrition-basis")?.reason.includes("source-to-site transformations, normalization, joins, deduplication and provenance"), "public-data nutrition research gate preserves transformation provenance");
check(editorialDecisions.decisions.find((item) => item.slug === "fishcake-soup-mealkit-basis")?.legacyDecision === "defer_research_needed", "fishcake-soup meal-kit owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "fishcake-soup-mealkit-basis")?.reason.includes("solids-eaten versus broth-consumed"), "fishcake-soup research gate preserves solid and broth consumption separation");
check(editorialDecisions.decisions.find((item) => item.slug === "gimbap-line-nutrition-basis")?.legacyDecision === "defer_research_needed", "whole-roll gimbap owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "gimbap-line-nutrition-basis")?.reason.includes("rice-versus-filling proportions"), "whole-roll gimbap research gate requires rice and filling proportions");
check(editorialDecisions.decisions.find((item) => item.slug === "cheese-protein-fat-basis")?.legacyDecision === "defer_research_needed", "cheese owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "cheese-protein-fat-basis")?.reason.includes("moisture and milk-fat classification"), "cheese research gate requires moisture and milk-fat classification");
check(editorialDecisions.decisions.find((item) => item.slug === "seaweed-snack-sodium-basis")?.legacyDecision === "defer_research_needed", "seaweed-snack owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "seaweed-snack-sodium-basis")?.reason.includes("iodine/mineral evidence"), "seaweed-snack research gate blocks unsupported iodine and mineral claims");
check(editorialDecisions.decisions.find((item) => item.slug === "porridge-soup-sodium-basis")?.legacyDecision === "defer_research_needed", "porridge/soup owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "porridge-soup-sodium-basis")?.reason.includes("porridge-versus-soup scope decision"), "porridge/soup research gate requires an explicit product-scope decision");
check(editorialDecisions.decisions.find((item) => item.slug === "dumpling-sodium-calorie-basis")?.legacyDecision === "defer_research_needed", "dumpling owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "dumpling-sodium-calorie-basis")?.reason.includes("wrapper/filling/dipping-sauce"), "dumpling research gate preserves wrapper, filling and dipping-sauce separation");
check(editorialDecisions.decisions.find((item) => item.slug === "rice-cake-snack-carbs-basis")?.legacyDecision === "defer_research_needed", "rice-cake snack owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "rice-cake-snack-carbs-basis")?.reason.includes("choking/age-safety"), "rice-cake snack research gate blocks unsupported choking and age-safety claims");
check(editorialDecisions.decisions.find((item) => item.slug === "beef-jerky-sodium-basis")?.legacyDecision === "defer_research_needed", "beef-jerky owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "beef-jerky-sodium-basis")?.reason.includes("moisture-concentration"), "beef-jerky research gate preserves the drying and moisture-concentration limit");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-udon-sodium-basis")?.legacyDecision === "defer_research_needed", "instant-udon owner remains non-public until its evidence gate is satisfied");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-udon-sodium-basis")?.reason.includes("actual-broth-intake"), "instant-udon research gate preserves the actual broth-consumption denominator");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("bagel-carbs-")).length === 5, "all five bagel-carbs rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("bagel-carbs-")).every((item) => item.mergeTarget === "phase6t-bagel-basis"), "bagel-carbs decisions merge into the phase6t bagel candidate");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-noodle-topping-")).length === 5, "all five phase6t noodle-topping rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-noodle-topping-")).every((item) => item.mergeTarget === "noodle-topping-calorie-basis"), "phase6t noodle-topping decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cheese-")).length === 5, "all five phase6t cheese rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cheese-")).every((item) => item.mergeTarget === "cheese-protein-fat-basis"), "phase6t cheese decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-oatmeal-")).length === 5, "all five phase6t oatmeal rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-oatmeal-")).every((item) => item.mergeTarget === "oatmeal-fiber-basis"), "phase6t oatmeal decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-jam-")).length === 5, "all five phase6t jam rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-jam-")).every((item) => item.mergeTarget === "jam-sugar-basis"), "phase6t jam decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-rice-cake-snack-")).length === 5, "all five phase6t rice-cake snack rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-rice-cake-snack-")).every((item) => item.mergeTarget === "rice-cake-snack-carbs-basis"), "phase6t rice-cake snack decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-coffee-drink-")).length === 5, "all five phase6t coffee-drink rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-coffee-drink-")).every((item) => item.mergeTarget === "coffee-drink-sugar-basis"), "phase6t coffee-drink decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-protein-bar-")).length === 5, "all five phase6t protein-bar rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-protein-bar-")).every((item) => item.mergeTarget === "protein-bar-fat-basis"), "phase6t protein-bar decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-instant-curry-")).length === 5, "all five phase6t instant-curry rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-instant-curry-")).every((item) => item.mergeTarget === "instant-curry-calorie-basis"), "phase6t instant-curry decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-table-sauce-")).length === 5, "all five phase6t table-sauce rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-table-sauce-")).every((item) => item.mergeTarget === "sauce-sodium-basis"), "phase6t table-sauce decisions merge into the earlier generic sauce owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-greek-yogurt-")).length === 5, "all five phase6t Greek-yogurt rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-greek-yogurt-")).every((item) => item.mergeTarget === "greek-yogurt-protein-basis"), "phase6t Greek-yogurt decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-soy-milk-")).length === 5, "all five phase6t soy-milk rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-soy-milk-")).every((item) => item.mergeTarget === "soy-milk-sugar-basis"), "phase6t soy-milk decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-triangle-kimbap-")).length === 5, "all five phase6t triangle-kimbap rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-triangle-kimbap-")).every((item) => item.mergeTarget === "triangle-kimbap-calorie-basis"), "phase6t triangle-kimbap decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => /^phase6t-cereal-(basis|mistake|compare|routine|faq)$/.test(item.slug)).length === 5, "all five phase6t cereal rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => /^phase6t-cereal-(basis|mistake|compare|routine|faq)$/.test(item.slug)).every((item) => item.mergeTarget === "cereal-sugar-basis"), "phase6t cereal decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-icecream-")).length === 5, "all five phase6t ice-cream rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-icecream-")).every((item) => item.mergeTarget === "icecream-satfat-basis"), "phase6t ice-cream decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-bakery-snack-")).length === 5, "all five phase6t bakery-snack rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-bakery-snack-")).every((item) => item.mergeTarget === "bakery-snack-serving-basis"), "phase6t bakery-snack decisions merge into the researched candidate owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-sports-drink-")).length === 5, "all five phase6t sports-drink rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-sports-drink-")).every((item) => item.mergeTarget === "sports-drink-sugar-basis"), "phase6t sports-drink decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-tteokbokki-kit-")).length === 5, "all five phase6t tteokbokki meal-kit rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-tteokbokki-kit-")).every((item) => item.mergeTarget === "tteokbokki-mealkit-sodium-basis"), "phase6t tteokbokki meal-kit decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-dried-sweet-potato-")).length === 5, "all five phase6t dried sweet-potato rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-dried-sweet-potato-")).every((item) => item.mergeTarget === "dried-sweet-potato-sugar-basis"), "phase6t dried sweet-potato decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-granola-")).length === 5, "all five phase6t granola rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-granola-")).every((item) => item.mergeTarget === "granola-fat-basis"), "phase6t granola decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-flavored-milk-")).length === 5, "all five phase6t flavored-milk rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-flavored-milk-")).every((item) => item.mergeTarget === "flavored-milk-sugar-basis"), "phase6t flavored-milk decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-meal-drink-")).length === 5, "all five phase6t meal-drink rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-meal-drink-")).every((item) => item.mergeTarget === "meal-replacement-drink-basis"), "phase6t meal-drink decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-powder-drink-")).length === 5, "all five phase6t powder-drink rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-powder-drink-")).every((item) => item.mergeTarget === "powder-drink-sugar-basis"), "phase6t powder-drink decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-fruit-veg-juice-basis")?.legacyDecision === "defer_research_needed", "fruit/vegetable juice basis is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-fruit-veg-juice-basis")?.candidateStatus === "research_needed", "fruit/vegetable juice defer records the missing product-category and label evidence");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-protein-shake-")).length === 5, "all five phase6t protein-shake rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-protein-shake-")).every((item) => item.mergeTarget === "protein-shake-sugar-basis"), "phase6t protein-shake decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-canned-corn-")).length === 5, "all five phase6t canned-corn rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-canned-corn-")).every((item) => item.mergeTarget === "canned-corn-sugar-basis"), "phase6t canned-corn decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-salad-dressing-")).length === 5, "all five phase6t salad-dressing rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-salad-dressing-")).every((item) => item.mergeTarget === "salad-dressing-sugar-basis"), "phase6t salad-dressing decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-mixed-nuts-")).length === 5, "all five phase6t mixed-nuts rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-mixed-nuts-")).every((item) => item.mergeTarget === "nuts-serving-size-basis"), "phase6t mixed-nuts decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-beef-jerky-")).length === 5, "all five phase6t beef-jerky rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-beef-jerky-")).every((item) => item.mergeTarget === "beef-jerky-sodium-basis"), "phase6t beef-jerky decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-porridge-")).length === 5, "all five phase6t porridge rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-porridge-")).every((item) => item.mergeTarget === "porridge-soup-sodium-basis"), "phase6t porridge decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cracker-")).length === 5, "all five phase6t cracker rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cracker-")).every((item) => item.mergeTarget === "cracker-sodium-basis"), "phase6t cracker decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-sandwich-")).length === 5, "all five phase6t sandwich rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-sandwich-")).every((item) => item.mergeTarget === "sandwich-sodium-basis"), "phase6t sandwich decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-seaweed-snack-")).length === 5, "all five phase6t seaweed-snack rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-seaweed-snack-")).every((item) => item.mergeTarget === "seaweed-snack-sodium-basis"), "phase6t seaweed-snack decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cup-noodle-")).length === 5, "all five phase6t cup-noodle rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cup-noodle-")).every((item) => item.mergeTarget === "cup-noodle-sodium-basis"), "phase6t cup-noodle decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-tofu-snack-")).length === 5, "all five phase6t tofu-snack rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-tofu-snack-")).every((item) => item.mergeTarget === "tofu-snack-protein-basis"), "phase6t tofu-snack decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-instant-udon-")).length === 5, "all five phase6t instant-udon rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-instant-udon-")).every((item) => item.mergeTarget === "instant-udon-sodium-basis"), "phase6t instant-udon decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-canned-tuna-")).length === 5, "all five phase6t canned-tuna rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-canned-tuna-")).every((item) => item.mergeTarget === "canned-tuna-sodium-basis"), "phase6t canned-tuna decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-ham-sausage-")).length === 5, "all five phase6t ham-sausage rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-ham-sausage-")).every((item) => item.mergeTarget === "ham-sausage-sodium-basis"), "phase6t ham-sausage decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-frozen-hotdog-")).length === 5, "all five phase6t frozen-hotdog rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-frozen-hotdog-")).every((item) => item.mergeTarget === "frozen-hotdog-satfat-basis"), "phase6t frozen-hotdog decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-frozen-dumpling-")).length === 5, "all five phase6t frozen-dumpling rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-frozen-dumpling-")).every((item) => item.mergeTarget === "dumpling-sodium-calorie-basis"), "phase6t frozen-dumpling decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-chicken-sausage-")).length === 5, "all five phase6t chicken-sausage rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-chicken-sausage-")).every((item) => item.mergeTarget === "chicken-sausage-sodium-basis"), "phase6t chicken-sausage decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-kimchi-")).length === 5, "all five phase6t kimchi rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-kimchi-")).every((item) => item.mergeTarget === "kimchi-sodium-label-basis"), "phase6t kimchi decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cold-lunchbox-")).length === 5, "all five phase6t cold-lunchbox rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-cold-lunchbox-")).every((item) => item.mergeTarget === "lunchbox-calorie-basis"), "phase6t cold-lunchbox decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-fried-rice-")).length === 5, "all five phase6t fried-rice rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-fried-rice-")).every((item) => item.mergeTarget === "fried-rice-meal-sodium-basis"), "phase6t fried-rice decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-fishcake-soup-kit-")).length === 5, "all five phase6t fishcake-soup-kit rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-fishcake-soup-kit-")).every((item) => item.mergeTarget === "fishcake-soup-mealkit-basis"), "phase6t fishcake-soup-kit decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-kids-drink-")).length === 5, "all five phase6t kids-drink rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-kids-drink-")).every((item) => item.mergeTarget === "kids-drink-100ml-basis"), "phase6t kids-drink decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-pasta-sauce-")).length === 5, "all five phase6t pasta-sauce rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-pasta-sauce-")).every((item) => item.mergeTarget === "pasta-sauce-sodium-basis"), "phase6t pasta-sauce decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-low-sodium-soup-base-")).length === 5, "all five phase6t low-sodium soup-base rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-low-sodium-soup-base-")).every((item) => item.mergeTarget === "soup-base-sodium-basis"), "phase6t low-sodium soup-base decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-instant-rice-")).length === 5, "all five phase6t instant-rice rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("phase6t-instant-rice-")).every((item) => item.mergeTarget === "instant-rice-carbs-basis"), "phase6t instant-rice decisions merge into the earlier topic owner");
check(editorialDecisions.decisions.find((item) => item.slug === "sandwich-sodium-basis")?.legacyDecision === "defer_research_needed", "sandwich-sodium owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "sandwich-sodium-basis")?.candidateStatus === "research_needed", "sandwich-sodium defer records its missing product and composition evidence");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("salad-topping-calorie-")).length === 5, "all five legacy salad-topping calorie rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("salad-topping-calorie-")).every((item) => item.mergeTarget === "phase6t-salad-topping-basis"), "legacy salad-topping calorie decisions merge into the researched candidate owner");
check(editorialDecisions.decisions.find((item) => item.slug === "protein-bar-fat-basis")?.legacyDecision === "defer_research_needed", "protein-bar-fat owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "protein-bar-fat-basis")?.candidateStatus === "research_needed", "protein-bar-fat defer records its missing product and sweetener evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "pudding-sugar-basis")?.legacyDecision === "defer_research_needed", "pudding-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "pudding-sugar-basis")?.candidateStatus === "research_needed", "pudding-sugar defer records its missing product, composition and storage evidence");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("dried-fruit-sugar-")).length === 5, "all five legacy dried-fruit sugar rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("dried-fruit-sugar-")).every((item) => item.mergeTarget === "phase6t-dried-fruit-basis"), "legacy dried-fruit sugar decisions merge into the researched candidate owner");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("pickle-sodium-")).length === 5, "all five legacy pickle-sodium rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("pickle-sodium-")).every((item) => item.mergeTarget === "phase6t-pickle-basis"), "legacy pickle-sodium decisions merge into the researched candidate owner");
check(editorialDecisions.decisions.find((item) => item.slug === "canned-corn-sugar-basis")?.legacyDecision === "defer_research_needed", "canned-corn owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "canned-corn-sugar-basis")?.candidateStatus === "research_needed", "canned-corn defer records its missing product, drained-weight and packing-liquid evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "cup-soup-sugar-basis")?.legacyDecision === "defer_research_needed", "cup-soup-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "cup-soup-sugar-basis")?.candidateStatus === "research_needed", "cup-soup-sugar defer records its missing product and preparation evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-cup-soup-basis")?.legacyDecision === "defer_research_needed", "phase6t cup-soup basis remains explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-cup-soup-basis")?.candidateStatus === "research_needed", "phase6t cup-soup defer preserves the narrower preparation-recalculation research state");
check(editorialDecisions.decisions.find((item) => item.slug === "oatmeal-fiber-basis")?.legacyDecision === "defer_research_needed", "oatmeal-fiber owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "oatmeal-fiber-basis")?.candidateStatus === "research_needed", "oatmeal-fiber defer records its missing product and preparation evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "cold-noodle-sauce-sugar-basis")?.legacyDecision === "defer_research_needed", "cold-noodle-sauce-sugar owner is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "cold-noodle-sauce-sugar-basis")?.candidateStatus === "research_needed", "cold-noodle-sauce-sugar defer records its missing broth-versus-bibim product and consumption evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "coffee-drink-sugar-basis")?.legacyDecision === "defer_research_needed", "coffee-drink-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "coffee-drink-sugar-basis")?.candidateStatus === "research_needed", "coffee-drink-sugar defer records its missing product, denominator and composition evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-oat-cup-sugar-basis")?.legacyDecision === "defer_research_needed", "instant-oat-cup-sugar owner is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "instant-oat-cup-sugar-basis")?.candidateStatus === "research_needed", "instant-oat-cup-sugar defer records its missing dry/prepared, liquid and topping evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "jam-sugar-basis")?.legacyDecision === "defer_research_needed", "jam-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "jam-sugar-basis")?.candidateStatus === "research_needed", "jam-sugar defer records its missing serving, sugar, fruit-content and storage evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "cracker-sodium-basis")?.legacyDecision === "defer_research_needed", "cracker-sodium owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "cracker-sodium-basis")?.candidateStatus === "research_needed", "cracker-sodium defer records its missing product, serving, nutrient and ingredient evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "jelly-drink-sugar-basis")?.legacyDecision === "defer_research_needed", "jelly-drink-sugar owner is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "jelly-drink-sugar-basis")?.candidateStatus === "research_needed", "jelly-drink-sugar defer records its missing product boundary, denominator and formulation evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "sauce-sodium-basis")?.legacyDecision === "defer_research_needed", "generic sauce-sodium owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "sauce-sodium-basis")?.candidateStatus === "research_needed", "generic sauce defer records its missing category, actual-use and product-label evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "kids-snack-sugar-basis")?.legacyDecision === "defer_research_needed", "kids-snack-sugar owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "kids-snack-sugar-basis")?.candidateStatus === "research_needed", "kids-snack defer records its missing age, category, intake and child-safety evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "drink-per-100ml-basis")?.legacyDecision === "defer_research_needed", "drink-per-100ml owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "drink-per-100ml-basis")?.candidateStatus === "research_needed", "drink-per-100ml defer records its missing beverage boundary, denominators and composition evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "salad-dressing-sugar-basis")?.legacyDecision === "defer_research_needed", "salad-dressing-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "salad-dressing-sugar-basis")?.candidateStatus === "research_needed", "salad-dressing defer records its missing category, actual-use, nutrient and emulsion evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "powder-drink-sugar-basis")?.legacyDecision === "defer_research_needed", "powder-drink-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "powder-drink-sugar-basis")?.candidateStatus === "research_needed", "powder-drink defer records its missing dry, prepared, added-liquid and composition evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "flavored-milk-sugar-basis")?.legacyDecision === "defer_research_needed", "flavored-milk-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "flavored-milk-sugar-basis")?.candidateStatus === "research_needed", "flavored-milk defer records its missing dairy identity, denominators and composition evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "dried-sweet-potato-sugar-basis")?.legacyDecision === "defer_research_needed", "dried-sweet-potato-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "dried-sweet-potato-sugar-basis")?.candidateStatus === "research_needed", "dried-sweet-potato defer records its missing product, intake, sugar and moisture evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "protein-shake-sugar-basis")?.legacyDecision === "defer_research_needed", "protein-shake-sugar owner is explicitly deferred rather than drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "protein-shake-sugar-basis")?.candidateStatus === "research_needed", "protein-shake defer records its missing format, preparation, nutrient and claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "kids-drink-100ml-basis")?.legacyDecision === "defer_research_needed", "kids-drink-100ml owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "kids-drink-100ml-basis")?.candidateStatus === "research_needed", "kids-drink defer records its missing age, category, intake, composition and child-safety evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "cereal-sugar-basis")?.legacyDecision === "defer_research_needed", "cereal-sugar owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "cereal-sugar-basis")?.candidateStatus === "research_needed", "cereal defer records its missing category, dry-serving, milk, nutrient and claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "sports-drink-sugar-basis")?.legacyDecision === "defer_research_needed", "sports-drink-sugar owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "sports-drink-sugar-basis")?.candidateStatus === "research_needed", "sports-drink defer records its missing category, intake, electrolyte, caffeine and claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "coconut-water-sugar-basis")?.legacyDecision === "defer_research_needed", "coconut-water-sugar owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "coconut-water-sugar-basis")?.candidateStatus === "research_needed", "coconut-water defer records its missing product, intake, sugar, electrolyte and claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "low-sugar-snack-basis")?.legacyDecision === "defer_research_needed", "low-sugar-snack owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "low-sugar-snack-basis")?.candidateStatus === "research_needed", "low-sugar-snack defer records its missing category, nutrition-claim, denominator and sweetener evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "ham-sausage-sodium-basis")?.legacyDecision === "defer_research_needed", "ham-sausage-sodium owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "ham-sausage-sodium-basis")?.candidateStatus === "research_needed", "ham-sausage defer records its missing product, intake, nutrient, ingredient and claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "greek-yogurt-protein-basis")?.legacyDecision === "defer_research_needed", "greek-yogurt-protein owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "greek-yogurt-protein-basis")?.candidateStatus === "research_needed", "Greek-yogurt defer records its missing identity, intake, composition, culture and health-claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "meal-replacement-drink-basis")?.legacyDecision === "defer_research_needed", "meal-replacement-drink owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "meal-replacement-drink-basis")?.candidateStatus === "research_needed", "meal-replacement defer records its missing identity, format, intake, nutrient and completeness-claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "canned-tuna-sodium-basis")?.legacyDecision === "defer_research_needed", "canned-tuna-sodium owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "canned-tuna-sodium-basis")?.candidateStatus === "research_needed", "canned-tuna defer records its missing product, packing-medium, denominator, nutrient and safety-claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "tofu-snack-protein-basis")?.legacyDecision === "defer_research_needed", "tofu-snack-protein owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "tofu-snack-protein-basis")?.candidateStatus === "research_needed", "tofu-snack defer records its missing identity, formulation, intake, nutrient, allergen and claim evidence");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("energy-drink-label-")).length === 5, "all five legacy energy-drink-label rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("energy-drink-label-")).every((item) => item.mergeTarget === "phase6t-energy-drink-basis"), "legacy energy-drink-label decisions merge into the researched Phase 6T candidate");
check(editorialDecisions.decisions.find((item) => item.slug === "granola-fat-basis")?.legacyDecision === "defer_research_needed", "granola-fat owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "granola-fat-basis")?.candidateStatus === "research_needed", "granola defer records its missing category, dry-serving, additions, nutrient, ingredient and claim evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "ready-side-dish-sodium-basis")?.legacyDecision === "defer_research_needed", "ready-side-dish-sodium owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "ready-side-dish-sodium-basis")?.candidateStatus === "research_needed", "ready-side-dish defer records its missing category, consumed-sauce, intake, nutrient, allergen and storage evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "frozen-hotdog-satfat-basis")?.legacyDecision === "defer_research_needed", "frozen-hotdog-satfat owner is explicitly deferred rather than broadly drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "frozen-hotdog-satfat-basis")?.candidateStatus === "research_needed", "frozen-hotdog defer records its missing product-form, cooking-state, intake, nutrient, ingredient and claim evidence");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("nutrition-serving-size-")).length === 5, "all five nutrition-serving-size rows have explicit cross-cluster decisions");
check(editorialDecisions.decisions.filter((item) => item.slug.startsWith("nutrition-serving-size-")).every((item) => item.mergeTarget === "nutrition-label-comparison-basis"), "nutrition-serving-size decisions merge into the public nutrition-label comparison owner");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-cold-noodle-sauce-basis")?.legacyDecision === "defer_research_needed", "cold-noodle sauce basis is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-cold-noodle-sauce-basis")?.candidateStatus === "research_needed", "cold-noodle sauce defer records the missing product and calculation evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-cereal-bar-basis")?.legacyDecision === "defer_research_needed", "cereal-bar basis is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-cereal-bar-basis")?.candidateStatus === "research_needed", "cereal-bar defer records the missing product and one-piece label evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-sparkling-water-drink-basis")?.legacyDecision === "defer_research_needed", "sparkling-water basis is explicitly deferred rather than broadly merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-sparkling-water-drink-basis")?.candidateStatus === "research_needed", "sparkling-water defer records the missing product-identity and ingredient-label evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-sweetener-snack-basis")?.legacyDecision === "defer_research_needed", "sweetener-snack basis is explicitly deferred rather than broadly merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-sweetener-snack-basis")?.candidateStatus === "research_needed", "sweetener-snack defer records the missing product, label and sweetener evidence");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-unsweetened-yogurt-basis")?.legacyDecision === "defer_research_needed", "unsweetened-yogurt basis is explicitly deferred rather than merged or drafted");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-unsweetened-yogurt-basis")?.candidateStatus === "research_needed", "unsweetened-yogurt defer records the missing product-boundary and ingredient-label evidence");
check(editorialDecisions.decisions.every((item) => item.candidateStatus !== "approved"), "dry-run decisions do not impersonate human approval");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-dried-fruit-basis")?.candidateStatus === "approval_candidate_pending_human_review", "dried-fruit basis is ready for human review");
check(driedFruitContract.status === "accepted_for_draft", "dried-fruit candidate has an accepted article contract");
check(driedFruitResearch.research_runs.length >= 3 && driedFruitResearch.research_runs.length <= 5, "dried-fruit candidate records 3-5 research runs");
check(driedFruitResearch.sources.length >= 5 && driedFruitResearch.sources.length <= 8, "dried-fruit candidate records 5-8 sources");
check(driedFruitResearch.sources.filter((source) => source.is_official).length >= 2, "dried-fruit YMYL candidate has at least two official sources");
check(driedFruitResearch.fact_traceability_pass === true && driedFruitResearch.unresolved_claims.length === 0, "dried-fruit fact traceability passes without unresolved claims");
check(driedFruitResearch.article_specific_details.length >= 2, "dried-fruit candidate has article-specific source details");
check(driedFruitBody.length >= 3500, "dried-fruit candidate body exceeds 3500 characters");
check((driedFruitBody.match(/^## /gm) || []).length >= 4, "dried-fruit candidate uses specific H2 sections");
check((driedFruitBody.match(/\]\(\//g) || []).length >= 3, "dried-fruit candidate includes at least three internal links");
check((driedFruitBody.match(/\]\(https:\/\//g) || []).length >= 5, "dried-fruit candidate includes at least five external citations");
check(!/당류을|건과일은를|원재료명을를|기준량을를/.test(driedFruitBody), "dried-fruit candidate removes known Korean particle defects");
check(driedFruitDraft.includes('humanReview: "pending"') && driedFruitDraft.includes("noindex: true"), "dried-fruit candidate remains pending and noindex");
check(driedFruitQa.body_characters === driedFruitBody.length, "dried-fruit QA records exact body length");
check(driedFruitQa.score >= 90 && Object.values(driedFruitQa.hard_gate_results).every((result) => result === "pass"), "dried-fruit QA score and hard gates pass");
check(saladToppingContract.status === "accepted_for_draft", "salad-topping candidate has an accepted article contract");
check(saladToppingResearch.research_runs.length >= 3 && saladToppingResearch.research_runs.length <= 5, "salad-topping candidate records 3-5 research runs");
check(saladToppingResearch.sources.length >= 5 && saladToppingResearch.sources.length <= 8, "salad-topping candidate records 5-8 sources");
check(saladToppingResearch.sources.filter((source) => source.is_official).length >= 2, "salad-topping YMYL candidate has at least two official sources");
check(saladToppingResearch.fact_traceability_pass === true && saladToppingResearch.unresolved_claims.length === 0, "salad-topping fact traceability passes without unresolved claims");
check(saladToppingResearch.article_specific_details.length >= 2, "salad-topping candidate has article-specific source details");
check(saladToppingBody.length >= 3500, "salad-topping candidate body exceeds 3500 characters");
check((saladToppingBody.match(/^## /gm) || []).length >= 4, "salad-topping candidate uses specific H2 sections");
check((saladToppingBody.match(/\]\(\//g) || []).length >= 3, "salad-topping candidate includes at least three internal links");
check((saladToppingBody.match(/\]\(https:\/\//g) || []).length >= 5, "salad-topping candidate includes at least five external citations");
check(!/견과류과|치즈을|치즈은|사람라면|열량로|기준 읽기을/.test(saladToppingBody), "salad-topping candidate removes known Korean particle defects");
check(saladToppingDraft.includes('humanReview: "pending"') && saladToppingDraft.includes("noindex: true"), "salad-topping candidate remains pending and noindex");
check(saladToppingQa.body_characters === saladToppingBody.length, "salad-topping QA records exact body length");
check(saladToppingQa.score >= 90 && Object.values(saladToppingQa.hard_gate_results).every((result) => result === "pass"), "salad-topping QA score and hard gates pass");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-salad-topping-basis")?.candidateStatus === "approval_candidate_pending_human_review", "salad-topping basis is ready for human review");
check(konjacJellyContract.status === "accepted_for_draft", "konjac-jelly candidate has an accepted article contract");
check(konjacJellyResearch.research_runs.length >= 3 && konjacJellyResearch.research_runs.length <= 5, "konjac-jelly candidate records 3-5 research runs");
check(konjacJellyResearch.sources.length >= 5 && konjacJellyResearch.sources.length <= 8, "konjac-jelly candidate records 5-8 sources");
check(konjacJellyResearch.sources.filter((source) => source.is_official).length >= 2, "konjac-jelly YMYL candidate has at least two official sources");
check(konjacJellyResearch.fact_traceability_pass === true && konjacJellyResearch.unresolved_claims.length === 0, "konjac-jelly fact traceability passes without unresolved claims");
check(konjacJellyResearch.article_specific_details.length >= 2, "konjac-jelly candidate has article-specific source details");
check(konjacJellyBody.length >= 3500, "konjac-jelly candidate body exceeds 3500 characters");
check((konjacJellyBody.match(/^## /gm) || []).length >= 4, "konjac-jelly candidate uses specific H2 sections");
check((konjacJellyBody.match(/\]\(\//g) || []).length >= 3, "konjac-jelly candidate includes at least three internal links");
check((konjacJellyBody.match(/\]\(https:\/\//g) || []).length >= 5, "konjac-jelly candidate includes at least five external citations");
check(!/당류을|곤약젤리은|사람라면|감미료을|열량로|기준 읽기을/.test(konjacJellyBody), "konjac-jelly candidate removes known Korean particle defects");
check(konjacJellyDraft.includes('humanReview: "pending"') && konjacJellyDraft.includes("noindex: true"), "konjac-jelly candidate remains pending and noindex");
check(konjacJellyQa.body_characters === konjacJellyBody.length, "konjac-jelly QA records exact body length");
check(konjacJellyQa.score >= 90 && Object.values(konjacJellyQa.hard_gate_results).every((result) => result === "pass"), "konjac-jelly QA score and hard gates pass");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-konjac-jelly-basis")?.candidateStatus === "approval_candidate_pending_human_review", "konjac-jelly basis is ready for human review");
check(bagelContract.status === "accepted_for_draft", "bagel candidate has an accepted article contract");
check(bagelResearch.research_runs.length >= 3 && bagelResearch.research_runs.length <= 5, "bagel candidate records 3-5 research runs");
check(bagelResearch.sources.length >= 5 && bagelResearch.sources.length <= 8, "bagel candidate records 5-8 sources");
check(bagelResearch.sources.filter((source) => source.is_official).length >= 2, "bagel YMYL candidate has at least two official sources");
check(bagelResearch.fact_traceability_pass === true && bagelResearch.unresolved_claims.length === 0, "bagel fact traceability passes without unresolved claims");
check(bagelResearch.article_specific_details.length >= 2, "bagel candidate has article-specific source details");
check(bagelBody.length >= 3500, "bagel candidate body exceeds 3500 characters");
check((bagelBody.match(/^## /gm) || []).length >= 6 && (bagelBody.match(/^## /gm) || []).length <= 8, "bagel candidate uses six to eight specific H2 sections");
check((bagelBody.match(/\]\(\//g) || []).length >= 3, "bagel candidate includes at least three internal links");
check((bagelBody.match(/\]\(https:\/\//g) || []).length >= 5, "bagel candidate includes at least five external citations");
check(!/탄수화물을를|베이글은를|크림치즈을|중량을를|기준 읽기을/.test(bagelBody), "bagel candidate removes known Korean particle defects");
check(bagelDraft.includes('humanReview: "pending"') && bagelDraft.includes("noindex: true"), "bagel candidate remains pending and noindex");
check(bagelQa.body_characters === bagelBody.length, "bagel QA records exact body length");
check(bagelQa.score >= 90 && Object.values(bagelQa.hard_gate_results).every((result) => result === "pass"), "bagel QA score and hard gates pass");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-bagel-basis")?.candidateStatus === "approval_candidate_pending_human_review", "bagel basis is ready for human review");
check(pickleContract.status === "accepted_for_draft", "pickle candidate has an accepted article contract");
check(pickleResearch.research_runs.length >= 3 && pickleResearch.research_runs.length <= 5, "pickle candidate records 3-5 research runs");
check(pickleResearch.sources.length >= 5 && pickleResearch.sources.length <= 8, "pickle candidate records 5-8 sources");
check(pickleResearch.sources.filter((source) => source.is_official).length >= 2, "pickle YMYL candidate has at least two official sources");
check(pickleResearch.fact_traceability_pass === true && pickleResearch.unresolved_claims.length === 0, "pickle fact traceability passes without unresolved claims");
check(pickleResearch.article_specific_details.length >= 2, "pickle candidate has article-specific source details");
check(pickleResearch.accessed === "2026-09-13" && pickleResearch.source_rechecked_at === "2026-09-13", "pickle current sources record the latest recheck date");
check(pickleResearch.article_specific_details.some((detail) => detail.inference === true && detail.evidence_limit), "pickle denominator rule is labeled as an editorial inference with an evidence limit");
check(pickleBody.length >= 3500, "pickle candidate body exceeds 3500 characters");
check((pickleBody.match(/^## /gm) || []).length >= 6 && (pickleBody.match(/^## /gm) || []).length <= 8, "pickle candidate uses six to eight specific H2 sections");
check((pickleBody.match(/\]\(\//g) || []).length >= 3, "pickle candidate includes at least three internal links");
check((pickleBody.match(/\]\(https:\/\//g) || []).length >= 5, "pickle candidate includes at least five external citations");
const pickleSourceUrls = new Set(pickleResearch.sources.map((source) => source.url));
const pickleCitedUrls = [...pickleBody.matchAll(/\]\((https:\/\/[^)]+)\)/g)].map((match) => match[1]);
check(pickleCitedUrls.every((url) => pickleSourceUrls.has(url)), "every pickle external citation is represented in the research packet");
check(!/(씻|헹구|물빼|물기 제거)[^\n]{0,40}\d+(?:\.\d+)?\s*%/.test(pickleBody), "pickle candidate makes no numeric rinsing or draining reduction claim");
check(pickleBody.includes("이 사이트의 보수적 계산 정책"), "pickle draft labels the denominator hold rule as editorial policy");
check(!/당류과|절임류을|피클은를|나트륨을를|기준 읽기을/.test(pickleBody), "pickle candidate removes known Korean particle defects");
check(pickleDraft.includes('humanReview: "pending"') && pickleDraft.includes("noindex: true"), "pickle candidate remains pending and noindex");
check(pickleQa.body_characters === pickleBody.length, "pickle QA records exact body length");
check(pickleQa.score >= 90 && Object.values(pickleQa.hard_gate_results).every((result) => result === "pass"), "pickle QA score and hard gates pass");
check(editorialDecisions.decisions.find((item) => item.slug === "phase6t-pickle-basis")?.candidateStatus === "approval_candidate_pending_human_review", "pickle basis is ready for human review");
check(bakeryContract.status === "accepted_for_draft", "bakery candidate has an accepted article contract");
check(bakeryResearch.research_runs.length >= 3 && bakeryResearch.research_runs.length <= 5, "bakery candidate records 3-5 research runs");
check(bakeryResearch.sources.length >= 5 && bakeryResearch.sources.length <= 8, "bakery candidate records 5-8 sources");
check(bakeryResearch.sources.filter((source) => source.is_official).length >= 2, "bakery YMYL candidate has at least two official sources");
check(bakeryResearch.fact_traceability_pass === true && bakeryResearch.unresolved_claims.length === 0, "bakery fact traceability passes without unresolved claims");
check(bakeryResearch.article_specific_details.length >= 2, "bakery candidate has article-specific source details");
check(bakeryBody.length >= 3500 && bakeryQa.body_characters === bakeryBody.length, "bakery body exceeds 3500 characters and QA records the exact length");
check((bakeryBody.match(/^## /gm) || []).length === 8, "bakery candidate uses eight specific H2 sections");
check((bakeryBody.match(/\]\(\//g) || []).length >= 3, "bakery candidate includes at least three internal links");
check((bakeryBody.match(/\]\(https:\/\//g) || []).length >= 5, "bakery candidate includes at least five external citations");
check(bakeryDraft.includes('humanReview: "pending"') && bakeryDraft.includes("noindex: true"), "bakery candidate remains pending and noindex");
check(bakeryBody.includes("무게 비례 추정") && bakeryBody.includes("속재료 분포"), "bakery draft labels the partial-portion calculation as an estimate with a composition limit");
check(!/크림빵은를|페이스트리을|기준량을를|중량로/.test(bakeryBody), "bakery candidate removes known Korean particle defects");
check(bakeryQa.score >= 90 && Object.values(bakeryQa.hard_gate_results).every((result) => result === "pass"), "bakery QA score and hard gates pass");
check(editorialDecisions.decisions.find((item) => item.slug === "bakery-snack-serving-basis")?.candidateStatus === "approval_candidate_pending_human_review", "bakery basis is ready for human review");

console.log(`editorial generation boundary: ${assertions.length} assertions passed`);
