# 2026-09-20 검토 후속 수정 — 로컬 검증

## 결과와 범위

사용자가 승인한 다음 작업 1~3번, 즉 감사 F-001~F-004를 구현했다. 공개 글의 내부 편집 지시와 구조 손실, 모바일 비교표, 검색·페이지·상세 왕복의 비교 선택을 다룬다. 나머지 감사 항목은 별도 이관 목록에 그대로 남는다. 서비스 배포나 AdSense 승인 완료를 뜻하지 않는다.

기준 HEAD는 `9f2c2d85b31950d58780b330e422208fcd93226c`이며 시작 시 추적 파일 변경은 없었다. 기존 미추적 작업과 감사 보고서는 보존했다. 현재 변경은 커밋/푸시하지 않은 로컬 작업이다.

## 구현

| 대상 | 변경과 이유 |
|---|---|
|승격기·공개 JSON|내부 `evidence_plan`/`non_overlap_claim`을 공개 표에 복사하지 않는다. 기존 승인된7개만 재생성하며 원문·승인·출처·발행 메타데이터를 보존한다.|
|글 블록·렌더러|문단, 표, 목록, 인용, 하위 제목, 본문 링크, 여러 줄 기록 양식을 순서대로 보존한다. 이전에 빠진 도입부와 짧은 문장도 복구한다. HTML/MDX 실행 없이 React 텍스트와 제한된 링크로 렌더링한다.|
|블로그·공통 CSS|글의 표를 의미 있는 `table/th/td`로 출력하고 표 내부만 가로 스크롤한다. 기존 기본 글의 비교표도 같은 의미 구조를 쓴다.|
|비교 화면|모바일 헤더 숨김 규칙을 제거하고 표 스크롤 영역을 둔다. 각 식품명·기준량·제거 링크가 접근성 트리에 남는다.|
|목록 선택|새 체크도 즉시 URL에 반영한다. 검색·페이지·다른 데이터셋·상세로 선택과 적용된 비교 기준/양을 전달한다. 목록에서 기존 선택을 해제·교체할 수 있다.|
|상세 링크|선택 상태는 URL을 읽는 작은 클라이언트 경계에 두고 영양 데이터 서버 결과에 섞지 않는다. 기존 선택에 항목을 추가하되 중복과 네 번째 항목을 막는다. 관련 상세와 목록 복귀는 검색어/페이지도 유지한다.|
|경계 보완|4개짜리 URL에서 해제할 때 숨겨진 네 번째 값이 다시 나타나는 오류를 브라우저에서 발견해 수정했다. 정규화한 표시 대상에 대해서만 추가/제거한다.|

새 의존성·의학적 주장·원자료 값·새 발행 승인·사이트 전체 저장소는 추가하지 않았다. 공개 표는 초안에서 보존하므로 내부 계약을 대신할 새로운 일반 문구를 만들어 넣지 않는다.

비교 기준/양은 기존처럼 **‘기준 적용’ 제출 시 확정**한다. 비교 화면의 ‘추가·교체’는 필터 없는 첫 페이지로 이동하면서 선택과 확정된 기준/양을 유지한다. 목록↔상세 및 브라우저 Back은 기존 검색어와 페이지를 유지한다. 상세의 선택 링크는 URL 상태가 준비되기 전에는 클릭 불가능한 자리표시자로 나타나 잘못된 단일 항목 링크로 이동하지 않는다.

## 검증 증거

증거 위치: `output/playwright/review-repair-20260920/`.

| 검사 | 결과와 범위 |
|---|---|
|새 공개 출력 회귀의 수정 전 실행|FAIL: bakery 글 내부 병합 지시 노출을 실제로 검출했다.|
|회귀 스크립트29개|PASS. 선택26개, 구조 파서10개 assertions와7개 공개 글 출력 검사 포함.|
|TypeScript / ESLint|PASS. `--noEmit --incremental false`, `eslint .`.|
|분리한 운영용 빌드|PASS. `next build --webpack`;23개 페이지 생성, 공개 블로그8개. 기존 `preferredRegion` 폐기 예정 경고만 남음.|
|글8개 ×360/390/1280px|24회 모두 PASS. 페이지 가로 넘침0, 원시 표/소제목 문법0, 확인 대상 내부 지시0. 신규 글13개 표와 기본 글1개 표 출력.|
|본문 보존 보완|베이글→샐러드 토핑 본문 링크와 피클 기록 양식의5개 줄을 실제 DOM에서 확인. 모든 초안 링크 목적지/코드펜스 수 보존 회귀 PASS.|
|검색→2페이지→상세→비교|PASS. 새 선택과 다른 페이지 선택, 150g/perIntake 보존. 상세에서 기존 선택을 지우지 않고 추가하며 중복 없음.|
|모바일 비교|360/390px에서 식품 헤더·제거 노출, 전체 페이지 넘침0. 첫 열/마지막 열 제거, 키보드 가로 스크롤, Back 복원 PASS.|
|추가 상태 경계|적용 기준 유지, reload/공유 URL, 기존 항목 교체, 교차 데이터셋, 네 번째 추가 차단, 전체 해제, 결과 없는 검색에서 복귀 PASS.|
|계산 불변|합성100g 자료를150g로 바꾸면 단백질48g, 나트륨2400/900mg. 이번 작업은 계산 함수를 변경하지 않음.|
|콘솔·네트워크|기록한 브라우저 콘솔 오류/경고0. 요청 목록의 host는 `127.0.0.1:3048`뿐. 운영 광고/CMP 검증은 아님.|
|소스 일치|변경된 제품 파일11개의 SHA256가 실행한 빌드 복사본과 일치. `source-manifest.json`.|

원기록: `checks.json`, `build-checks.json`, 개별 검사 로그, `browser-articles.log`, `browser-selection1.log`~`browser-selection4.log`, `browser-proof.json`, `browser-console.log`, `browser-requests.log`. 화면은 `article-360.png`, `compare-360.png`, `compare-last-column-360.png`에 보존했다.

실패와 복구도 구분한다. 첫 분리 빌드는 C: 임시 디렉터리와 E: 의존성 사이의 webpack 경로 문제로 실패했으며, 동일 드라이브 복사본으로 재실행해 통과했다. 첫 브라우저 helper는 CLI 실행 공간에 전역 `URL`이 없어 실패했으므로 페이지 컨텍스트에서 읽도록 고쳤다. 실제 기능 실패였던 네 번째 선택 부활은 제품 코드와 회귀 테스트를 고친 뒤 같은 브라우저 흐름으로 재검증했다. 키보드 스크롤 검사는 애니메이션 전 즉시 값을 읽어 한 번 실패했으므로 실제 scrollLeft 변화까지 기다리도록 수정해 통과했다.

## 실행하지 않은 검증과 남은 항목

- 기존31개 스크립트 중 별도 이력 보고서를 다시 쓰는 `test-editorial-corpus-map.mjs`, `test-editorial-contract-risk-priority.mjs`는 이번 수정 범위 밖이라 실행하지 않았다. 나머지29개는 실행했다.
- 실제 운영 DB/API, 운영 사이트 반영, AdSense/CMP/GA 계정, 규정 전문 검증은 실행하지 않았다. 건강 영양 실패·건수 의미·아연 원자료·극단 계산 무한대 등 F-005 이후 결함은 이번 완료 범위에 포함하지 않는다.
- 자동 탐색 상태 보존은 JavaScript가 동작하는 브라우저에서 검증했다. 목록의 기본 비교 form은 서버 제출 경로를 유지하지만 전체 무JavaScript 왕복을 완료로 주장하지 않는다.
- 영양표의 정확성은 합성 자료 계산 보존 범위로 확인했다. 실제 제품값과 라벨의 일치는 별도 검증이다.

## 독립 검토

Spark 시작이 `Unknown model`로 실패해 같은 범위의 Luna(max) 검토로 각각 한 번 재시도했다. 비교 선택 검토는 현재 동작 경계에서 필수 수정이 없다고 판단했다. 지적된 Back 직후 검색어/페이지 증거는 실제 브라우저 assertion을 추가해 통과했다. 공개 출력 검토가 찾은 본문 링크·기록 양식 손실은 수정했고 재검토에서 남은 중대한 결함이 없다는 판정을 받았다. 최종 빌드·브라우저 재실행은 주 에이전트가 별도로 확인했다.

## 재실행·복구

- 회귀/타입/lint: `node scripts/verify-review-repair.mjs --checks`.
- 분리 빌드/서버: `node scripts/verify-review-repair.mjs`. 환경 키와 광고/분석 플래그를 제거한 자식 프로세스를 사용하고 `.env`를 복사하지 않는다. 이 증거 디렉터리의 합성 SQLite fixture가 있어야 한다. 생성한 별도 디렉터리는 `runtime.json`에 기록한다.
- 브라우저: 서버3048이 실행된 상태에서 별도 Playwright CLI 세션을 열고 `browser-articles.txt`, `browser-selection1.txt`~`browser-selection4.txt` 순서로 `run-code --filename`을 사용한다. selection2는 앞 단계의 최종 화면을 사용하고 나머지는 독립 진입한다.
- 롤백: 이 작업의 Git diff와 새로 추가된 제품/테스트 파일만 되돌린다. 기존 감사·미추적 작업·다른 실행 서버는 보존한다. 외부 배포가 없어 원격 롤백은 없다.

선택 상태 구현에서 사용한 History API 연동은 [Next.js 공식 문서](https://nextjs.org/docs/app/getting-started/linking-and-navigating#native-history-api)로 확인했다.

## 완료 점검 — 2026-09-20T08:05:36+09:00

수락 기준 A1–A6를 충족했고 현재 작업 파일 11개의 해시가 최종 빌드와 일치한다. `git diff --check`도 통과했다. 최종 점검은 `output/playwright/review-repair-20260920/completion-audit.json`에 기록했다.

작업용 브라우저와 3048 서버는 종료했으며 기존 31847 서버는 보존했다. 빌드 복사본과 합성 SQLite 자료는 재현 증거로 남겼다. 최신 빌드 복사본 경로는 `runtime.json`을 따른다.

다음 로컬 작업은 F-006의 저장 건수·원천 전체 건수·미확인 상태 구분이다. F-005의 조회 실패/정상 0건 분리, F-010의 극단 입력 계산 방어, F-008의 사이트맵 정합성도 남아 있다. 이들은 이번 완료 범위에 포함되지 않는다.

배포 기록 참고: 위 `output/` 원기록과 `docs/audits/` 감사 자료는 로컬 보존 증거이며 GitHub 푸시 대상에 포함하지 않는다. 이 요약 문서와 제품·회귀 코드는 배포 커밋에 포함한다.
