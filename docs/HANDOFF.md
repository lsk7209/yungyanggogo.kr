# Current Handoff

- Timestamp: 2026-08-30 10:22:08 +09:00
- User goal: Review and optimize the managed SEO fleet one site at a time, using current first-party evidence and GitHub-first inspection where required.
- Exact current state: Task commit `da53b82` is on GitHub `main`; its Git-connected Vercel status completed successfully and its CI verify job passed. That push exposed a second legacy path in `ci.yml`: it automatically submitted the sitemap after every `main` push. The legacy job entered its submit-and-poll step once, then run `33285359501` was cancelled after the submission to stop unnecessary polling. This checkout now removes that push-triggered submit job before a follow-up commit. Public home, robots, sitemap, representative nutrition page, trust pages, invalid URL behavior, AdSense loader, and ads.txt were independently verified healthy on 2026-08-30. Search Console reported 2,832 submitted URLs, zero warnings or errors, and `pending=false` before that one automatic submission.
- Completed work: Changed the scheduled GSC workflow from an hourly unconditional sitemap submission with a `pending=false` gate to a daily read-only live/status check. Sitemap submission is now available only through an explicit manual boolean input that defaults to `false`. Added read-only workflow permissions and concurrency control while preserving the repository's existing `npm install` behavior. Removed the duplicate `main`-push GSC submission job from `ci.yml`, added explicit read-only token permission there, and kept normal typecheck/build CI intact.
- Changed files: `.github/workflows/gsc-sitemap-scheduled.yml`; `.github/workflows/ci.yml`; `docs/HANDOFF.md`.
- Validation evidence: Both YAML files parse and ten combined workflow assertions pass: daily read-only status monitoring, manual submission defaulting false, least-privilege tokens, and CI containing only the normal verify job with no submit command. The real read-only commands `gsc:sitemap:check-live` and `gsc:sitemap:status` passed; the latter confirmed site-owner visibility, 2,832 submitted URLs, zero warnings/errors, and `pending=false`. `npm run typecheck` passed. `npm run build` compiled successfully and generated 415 static pages. First-push GitHub CI typecheck/build passed and Git-connected Vercel deployment completed. Full `npm run lint` remains red only on two pre-existing unescaped-quote errors in `app/page.tsx:172` plus one pre-existing image warning; neither file is part of this change. `npm ci` was also tested and correctly rejected the repository's currently inconsistent lockfile, so workflows retain `npm install`. Diff and secret checks passed, and remote `main` still matches local HEAD `da53b82`; follow-up commit/push and final GitHub check remain pending.
- Side effects and rollback: Commit `da53b82` triggered the normal Git-connected deployment and one legacy automatic Search Console sitemap submission before that path was discovered; the remaining polling run was cancelled. No manual dispatch or repeat submission was sent. After the follow-up lands, rollback is a normal revert of the two task commits. The daily workflow continues to read the existing `GSC_CREDENTIALS_JSON` secret but scheduled runs no longer submit the sitemap.
- Blockers or risks: `package-lock.json` is not currently suitable for `npm ci`, and the repository dependency install reports known audit findings; lockfile/dependency remediation is outside this bounded workflow repair. A manual dispatch with `submit=true` still mutates Search Console and must remain an intentional operator action.
- Deliberately not run or sent: No Search Console submission, workflow dispatch, production database write, API backfill, content generation, content publication, Vercel CLI/API command, analytics mutation, AdSense mutation, or application source change.
- Single next step: Validate both workflow contracts, verify GitHub `main` has not drifted, then commit and push only `ci.yml` plus this handoff update and confirm that the new CI run contains `verify` but no GSC submit job.
